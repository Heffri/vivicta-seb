# v128 — docs: the acrylic README catches up to v114–v123 (rows + current KB/eval numbers); the ework label follows v078's dash-to-0 ruling

Lane `ao/vivicta-glm-70/v128-docs-rows-v114-v123`, off `origin/acrylic` at dispatch
`8a0e784`. Docs + one label row only — zero code, zero model calls. Everything below
ran offline on the dispatch tree.

## README

**New rows** (end of the "Backend hardening" table, after v104; wording in the table's
existing style, every claim traceable to the linked evidence):

- **v114/v115/v118/v119 — one merged row** (they are re-run batches): the whole
  104-company Mid Cap universe re-run under the current stack, four batches two passes
  each, publish policy = nothing loses against `eval/labels.csv`; links all four
  evidence files. The row also names the three finds the batches dispatched: the
  exhausted Mid Cap pool, the `modern_times_2025` corpus defect (→ v122), and the
  ework dash label↔mechanism collision (→ the label ruling below).
- **v116** — the 15 documented-wrong/empty stems re-extracted: 9/15 companies on their
  labels, 27/36 fields, 17 guard refusals all genuinely wrong tables, residual =
  locator (5) + one model decline.
- **v120** — prompt A/B, docs only: +4 fields but 1 ✓→✗ → per the pre-set rule not
  committed; the finding is reachability (14 of 20 companies' label pages outside
  `candidate_pages`), which dispatched v123.
- **v121** — `LLM_STRICT_SCHEMA`: codex `--output-schema` file vs claude `--json-schema`
  inline, one flag-less fallback retry, default off, byte-identical on stable reports.
- **v122** — fetch's year validation anchored to cover/first-3-pages + accounting-period
  statements; `scripts/kb_year_audit.py` (206 checked, 3 suspicious); issuer check
  accepts the company's own cover acronym; MTG re-fetched (FY2025, 184 p) + re-extracted.
- **v123** — `scripts/locate_reach.py` + the three metric-gated locator changes; debt
  label-page reachability **66/87 → 83/87**, income 101/101, zero companies moved down.

**v117 was not re-added**: it already has its row in the Frontend table
(Window caption buttons follow theme and tone, `evidence/v117.md`).

**Numbers updated** ("What a colleague sees now"), each re-derived on this tree, not
copied:

| Number | README said | Now | How counted |
|---|---|---|---|
| KB size | 196 companies | **206** | `ls data/kb \| wc -l` |
| stored `debt_maturity` extractions | 95 | **105** | `ls data/kb/*/extractions/debt_maturity.json \| wc -l`; 104 Mid Cap + ericsson |
| offline eval values | 257/327 (78.6%) | **270/327 (82.6%)** | `python eval/run.py --stored-kb data/kb`, run on this tree |
| offline eval pages | 200/327 (61.2%) | **209/327 (63.9%)** | same run |

The label file itself re-verified at **231 `debt_maturity` rows across 87 companies**
(+99 income rows, of which 96 score — `nordic_industrials_2025` is the fixture
placeholder with no stored extraction; 231 + 96 = the 327 scored rows the eval prints).
The work order's "106 Mid Cap with debt_maturity" did **not** verify: the counted number
is 105 stems total (104 Mid Cap non-Real-Estate + ericsson), the same 105 v114 counted
— the README prints the counted numbers.

## The ework label ruling (supervisor, 2026-09-20)

v119's recorded collision, resolved: v078's dash-to-0 ruling (a row whose own printed
figures close to its total — Ework 156,409 + 0 + 0 ≈ 156,410 within ±2 — makes the
printed dash that bucket's explicit 0, `printed_nil`) is later and more specific than
v068's label note (dash = unprinted, not a fabricated zero), and the label follows the
mechanism. `eval/labels.csv`'s one ework row changed — `due_1_to_5_years`: expected
`null` → **`0`**, notes keep v068's column-order explanation verbatim and replace the
"null (not 0)…" sentence with the ruling text (naming v078, the arithmetic, and the
superseding ruling). The stored `data/kb/ework_2025` extraction already holds
`due_1_to_5_years = 0 @ 0.5` (check passed), so no republish is needed.

## Label regression and eval, before → after

- `python scripts/label_regression.py --schema income_statement --schema
  debt_maturity --baseline origin/acrylic`: **identical=899, changed=8, skipped=430
  before, byte-identical totals after** (the 8 are the documented duni/enea/medcap×2/
  nobia×2/rejlers/rusta first-scoring asymmetry set, present before this lane; ework is
  identical=4 / changed=0 in both runs — the tool compares label *matching*, which the
  value edit does not touch).
- `python eval/run.py --stored-kb data/kb`: values **269/327 (82.3%) → 270/327
  (82.6%)**, pages **209/327 (63.9%)** unchanged. A full-output diff shows exactly one
  scored row moved — `ework due_1_to_5_years` ✗ → ✓ (2/3 → **3/3**), dropping out of the
  misses list — and **zero other companies changed**.

## Housekeeping

`git diff 8a0e784 --stat` = `docs/acrylic/README.md` (+8/−2), `eval/labels.csv` (1 row),
this file. No `backend/`, `frontend/`, `scripts/`, `desktop/`, `data/` paths; working
tree otherwise clean. Owner-identifier grep over every changed/new file — empty. The
two commits: `docs: acrylic README rows for v114-v123 and the current KB/eval numbers`
(README + this file) and `eval: ework due_1_to_5_years follows v078's
printed-dash-is-zero ruling` (labels.csv), pushed as
`ao/vivicta-glm-70/v128-docs-rows-v114-v123` to `origin` only.
