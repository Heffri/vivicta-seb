# v042 — docs wrap-up: desktop app, four model providers, two hardening rounds, a new assumption

Lane `v042-notes`, off `origin/acrylic` (`9086790`). Docs only, no code — `docs/acrylic/README.md`,
root `README.md`, this evidence file.

## What changed

`docs/acrylic/README.md`:

- New **"Desktop app"** section (after Frontend, before Backend hardening): what the Electron shell
  is, dev vs. packaged mode, real Windows 11 acrylic material + the draggable titlebar, the two
  unsigned packages (SmartScreen warning), the userData-relative data directory, first-launch
  demo-mode default, and a 3-command build pointer to `desktop/README.md`. Sourced from
  [v029](v029.md), [v030](v030.md), [v032](v032.md), [v038](v038.md) — no new numbers invented,
  package sizes and exact timings deliberately left out since the work order didn't ask for them.
- New **"Model providers"** section: the four providers (Local Ollama, an OpenAI-compatible endpoint
  incl. Anthropic-compatible, Codex CLI, Claude CLI) as a `LLM_PROVIDER`/env table, the desktop
  Settings-tab round trip, the embeddings-always-OpenAI-compatible limitation, and the Atlas Copco
  verification result (Codex 9/9 fields, Claude 9/9 fields, both 0 warnings). Sourced from
  [v031](v031.md), [v033](v033.md), [v039](v039.md).
- **"Backend (model-free hardening)" renamed to "Backend hardening"**, intro sentence changed from
  "nothing below calls a model" to naming the four new rows as the exception — that claim would have
  become false the moment the new rows landed, since the `debt_maturity` hardening-loop rows call a
  real Codex CLI model to find failures (the fix that ships is still deterministic and verified with
  no further calls). Table gains 4 rows: v035 (seed 1, 3 already-correct / 1 known locator gap left
  alone / 2 schema fixes / 4 hit a new column-bucket gap), v036 (the general column-bucket reader that
  fixed 1 of those 4), v037 (seed 2, full-confidence count unchanged, 3 schema commits that moved no
  number this seed, 2 real bugs found and left for another lane), v040 (fixes those 2 bugs — Acast's
  column-bucket mechanism reading the wrong table, Nelly's unguarded neighbour-sum). Added a one-line
  caveat under the table: a single before/after model call isn't proof of a fix or a regression
  (v037's own re-run moved two unrelated numbers with zero code change), so anything below a full pass
  needs a repeated-trial check. Sourced from [v035](v035.md), [v036](v036.md), [v037](v037.md),
  [v040](v040.md) — the "3/1/6" and "1/7/2" shorthand from the work order is spelled out in prose
  rather than repeated as bare digits, since the source evidence itself splits those groups a little
  differently depending on which axis you count by (e.g. Acast appears both in v037's "2 real bugs"
  list and its "6/10 granularity pattern" list); the prose breakdown here matches what v035/v037 state
  directly, not an invented reconciliation of the two framings.
- **New 4th "Assumptions to confirm"** item: finer bucket splits (`1-2y`/`2-5y`, or a bare
  current/non-current split) are summed into the three schema buckets rather than kept as their own
  fields, per `value_convention` (quoted in spirit, not verbatim) and `extract.py`'s column-bucket
  reader (v036/v040). Flags that keeping the 3-bucket shape vs. splitting the schema is Kristian's
  call, and that splitting would also touch `ppt.py`'s `BUCKET_ORDER`/`BUCKET_LABELS` and the
  frontend's maturity charts, not just the schema — same scope note [v035](v035.md) itself made.
- **"How to verify"** gains `npm run e2e`, `test_kb`, `test_paths`, `test_llm` (all confirmed to
  exist — `frontend/package.json`'s `e2e` script, `backend/pipeline/test_kb.py`, `test_paths.py`,
  `test_llm.py` — before adding, not assumed from the work order's wording).

Root `README.md`: one new "Desktop app" section (3 lines of body text) between "Run it" and "How a
section works", pointing to `desktop/README.md`.

## Line count

`docs/acrylic/README.md`: 178 → 264 lines (cap was 300). Read through once after the edits (caught
and fixed one grammar slip: "is draggable and double-click-to-maximizes/restores" →
"is draggable and supports double-click-to-maximize/restore").

## Verification

- Every fact/number above traced to the named evidence file at the point of writing — no invented
  numbers (`git diff` of `docs/acrylic/README.md` was reread against the source `v029`–`v040` files
  listed above before delivery).
- `backend/schemas/debt_maturity.json` read directly to phrase the new assumption item's description
  of `value_convention` accurately rather than paraphrase from memory of v035/v037's evidence alone.
- `frontend/package.json` and `backend/pipeline/*.py` filenames checked to exist before citing them
  in "How to verify".
- No internal jargon, no owner identifier, no local path introduced — this branch only ever touches
  two doc files plus this evidence file, so the check is `git diff --name-only origin/acrylic | xargs
  grep -il` for the owner identifier: empty.
- Territory: `docs/acrylic/README.md`, root `README.md`, this file. No code touched (`git diff
  --stat` against `origin/acrylic` shows exactly those two non-evidence files).

## Owner-identifier check

`git diff --name-only origin/acrylic | xargs grep -il` (per `common.md`) → empty.
