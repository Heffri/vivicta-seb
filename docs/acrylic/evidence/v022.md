# v022 — docs: acrylic notes backfilled through v021 (evidence)

Branch `ao/vivicta-seb-17/v022-notes-update`, off `origin/acrylic` @ `d6ff145` (latest at dispatch; acrylic
hadn't moved by delivery, no rebase needed). Territory: `docs/acrylic/README.md` and this file only, no code
touched. Read `v015`, `v017`–`v021` and their screenshot directories (`v017`/`v018` are backend-only, no
screenshots) plus the existing `v008` row before editing. Folded each into the established table-row
convention rather than appending new View rows: Extract's row gained the upload-title wrap fix and the
multi-file drop, flagged — in both the row and the "Unchanged" paragraph — as the one place view logic
actually changed (`file` state became `files[]`); Results and Compare picked up the header-eyebrow dedup;
Compare also gained the failure next-step line and the maturity mini-bar, worded to make clear it reuses
`MaturityChart`'s bucket rule and identity check rather than re-deriving them; Knowledge base gained the
PDF-availability badges, the split header count and the "With PDF only" toggle (including its no-op
fallback); Cross-cutting absorbed the four-more-call-sites focus-ring pass. The backend table's `v008` row
was rewritten from "two follow-ups" to "three," adding the reverted `title_keywords` heading-gate experiment
alongside `exclude_keywords` and the neighbour bonus, with its own numbers (12/101 flipped, 4 better / 7
worse / 1 neutral, all three experiments failing for the same reason — no fixed heading-keyword list
separates "Liquidity risk"-titled maturity tables from "Liquidity risk"-titled narrative pages) folded into
the "Numbers behind the table" paragraph beside the other two; a new row documents `v018`'s footnote-glue
fix. Two Contract-gaps edits went beyond literal instruction-following because the evidence disagreed with
the README's current text: the old `_clean_label`-footnote-marker bullet (sourced to `v014`) is gone because
`v018` fixed exactly that gap — the backend table and its numbers paragraph now say so, and the stale
"735/736, one inert change" claim is corrected to "739/739, 0 changed" (v018's own regression run, which
covers `income_statement` and `debt_maturity` together) — replaced by the dormant magnitude-prefixed-unit
gap (`MSEK`/`TSEK`/`KSEK`/`MEUR`) `v018` found instead; the fixture `/extract` bullet was widened to cover
"which file," not just "which section," since `v021` reconfirmed the same pre-existing behavior on a
multi-upload rather than surfacing a distinct new gap. No premise was overturned. Gates: docs-only, no
build/lint/test applies; README grew from 157 to 181 lines (cap was 220), +49/-25 lines by
`git diff --stat origin/acrylic -- docs/acrylic/README.md`; every added evidence link
(`v015`/`v017`/`v018`/`v019`/`v020`/`v021`, including the `§`-numbered ones) resolves to a real file or
in-file heading; `git diff --name-only origin/acrylic -- docs/acrylic/README.md docs/acrylic/evidence/v022.md
| xargs grep -il <owner identifier>` → empty.
