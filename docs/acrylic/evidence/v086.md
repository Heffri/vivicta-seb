# v086 — README catch-up rows (v074–v085) + "how a teammate gets it"

Lane `ao/vivicta-glm-29/v086-docs-rows`, off `origin/acrylic` (`0a09a63` at dispatch; re-fetched
immediately before delivery — no movement). Docs only — `docs/acrylic/README.md` and this file; no
other file touched (`git diff --stat` vs baseline shows exactly these two markdown files).

- **Eleven Backend-hardening rows appended** after v073's, in lane order — v074, v076, v077, v078,
  v079, v080, v081, v082, v083, v084, v085 — every number taken from that lane's own evidence file
  (4/5 foreign fetch; Ework 156 409 @1.0, then dash-zeros → check passed; desktop 0.3.3 + the
  `desktop-0.3.3` prerelease; clean clone 156 s; fifth-source IR crawl + `_filename_clear`; 6/7
  foreign status, Shell standing; seed 8 full 0 / partial 8 / failed 2; `row_synonyms` migration +
  the four synonym keys; Tången fiscal-year narrowing → passed; Instalco second header tier, the
  `over`-valve blocker named one layer down).
- **Top of the README** ("How a teammate gets it", after the existing summary): Release exe
  double-click / clone the `demo` branch + `run.bat` / Settings → Codex or Claude provider, no API
  key. The release tag, prerelease flag and both assets verified read-only against the team repo's
  Releases before writing the sentence.
- **Desktop section**: confirmed **0.3.3** built from `0f2e2bc` (v077's own sentence, checked against
  `v077.md` §1/§3, unchanged) and added one sentence naming the `desktop-0.3.3` prerelease.
- **Hardening-loop intro refreshed**: seven rounds → **eight** (seeds 1–8), 67 → **77** company draws
  (67 + seed 8's ten), [v082] joined the round links, "seeds 7 and 8 measured the merged stack and
  deliberately landed nothing".
- Checks: the table renders one row per lane with evidence links, columns intact; owner-identifier
  grep over the full diff vs baseline — empty, run as its own step before pushing; docs-only diff,
  so the build/lint/test gates are not exercised.
