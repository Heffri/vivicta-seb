# w206 — packaged demo rehearsal

Branch `ao/vivicta-seb-125/w206-demo-package`, based on `origin/acrylic` at
`0dff6d1`. Scope is limited to the isolated package rehearsal, `docs/DEMO.md`,
this evidence, and the five renderer screenshots under `w206/`. No product code,
API contract, evaluation data, shared package, or model provider was changed or
called.

## Package freshness — red, then green

The available shared unpacked package was red: its `BUILD.txt` named `b3d56`,
not the current acrylic tip. It was copied to a lane-local package location and
refreshed there only. The refreshed copy's marker is
`0dff6d14dfc55b46854db86d38eb008c3b31c929`, built at 14:07 on 2026-09-22.
The shared target was not changed.

The fresh package was launched with a newly created, quoted user-data directory
whose path contains a space. It used fixture mode, made no real model calls, and
returned **206** saved reports from `/api/kb` in **11.33 s**. The UAW `demo` tree
was consulted as the reference source; no UAW code was copied because this lane
changes no product code.

## Rehearsal record

| Step | Measured time | Observed result |
|---|---:|---|
| 1. First launch → `/api/kb` | 11.33 s | 206 saved reports from the clean user-data directory. |
| 2. Extract landing → **Open a real debt sample** | 0.137 s | Karnell Group's stored result opened; no extraction or model request. |
| 3. Karnell **Due 1–5 years** → source | 0.097 s | Page 106 and the highlighted quote `Liabilities to credit institutions 43.5 353.7 - 397.2`. The package has saved text, not the original PDF. |
| 4. KB → All → Ependion → **Due after 5 years** | 0.206 s | The source panel says **Not printed in this report**, visibly distinct from zero (page 155). |
| 5. Review Ependion total borrowings | ≤1.1 s UI run | Saved a human confirmation against page 155: `Borrowing 167,546 35,636 380,348 583,531`. |
| 6. KB **Export all** | CSV 0.266 s; PPTX 1.805 s | CSV: HTTP 200, 26,507 bytes, 105 data rows (quoted source fields can contain line breaks). PPTX: HTTP 200, 536,326 bytes, opened by `python-pptx` with 110 slides. |
| 7. Compare Karnell + Ependion → All maturity wall | 0.295 s + 0.202 s | Fully loaded: 0/105 comparable, 105 basis-unconfirmed, 13 missing totals, 49 missing `<1y`. |
| 8. Wallenberg directory: Sarnova | 0.153 s | Per the w206 ruling, used Extract's company directory: Sarnova showed `Private company — reported inside Investor AB's annual report (Patricia Industries)` and opened Investor AB at page 41. Renderer requests contained no `/discover`, `/fetch`, or `/extract`. Company map currently lists stored-report owners and does not render private roster members as selectable; its column treatment is out of scope. |
| 9. Atlas Copco exact upload | 9.1 s | Fixture upload completed with the batch row visibly **Done**; fixture output is clearly demo data. |
| 10. Close → reopen persistence check | close 0.11 s; reopen 2.99 s | `/api/kb` returned HTTP 200 with 207 records after the Atlas fixture upload. Renderer UI re-observed **All companies** selected, light tone, and Ependion's `Human confirmed` / `Demo rehearsal` review. |

The owned app, backend, and dedicated debug port were closed again after the second renderer
verification. The initial launch-policy rejection was worked around with the approved `cmd`
launch route; no application failure, renderer error, or model call was observed.

## Visual evidence

Each image is a renderer capture from the packaged application, visually inspected after capture;
none is a full-desktop capture.

- `w206/karnell-source-quote.png` — selected Karnell bucket, page 106 and highlighted quote.
- `w206/ependion-not-printed.png` — explicit **Not printed in this report** absence.
- `w206/ependion-review-saved.png` — saved Ependion human confirmation with source context.
- `w206/compare-upcoming-maturities.png` — populated All-collection upcoming-maturities wall.
- `w206/atlas-upload-done.png` — Atlas Copco fixture-upload row marked **Done**.

## Gates

The package exercise created no tracked `data/` mutation.

- `frontend/npm run build` — pass; the existing non-fatal >500 kB chunk advisory remained.
- `frontend/npm run lint` — pass with 13 existing warnings.
