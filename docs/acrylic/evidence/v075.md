# v075 — README catch-up rows (v071/v072/v073), hardening-loop summary

Lane `ao/vivicta-glm-26/v075-docs-rows`, off `origin/acrylic` (`8cc42fc` at dispatch; re-fetched and
re-based before delivery per the standing rule). Docs only — `docs/acrylic/README.md` and this file;
no other file touched (v074/v076 were changing code concurrently).

- **Three Backend-hardening rows appended** after v069's: v071 (desktop **0.3.2**, build `e216fd4`,
  acceptance (a)–(i) over CDP), v072 (seed 7: 7 companies × 2 passes, full 0 / partial 4 / failed 3,
  two-pass 7/7, 44-of-50 Codex calls incl. 16 timeout duplicates → the standing never-reissue driver
  rule, no schema/pipeline change landed), v073 (third maturity-note shape — per-instrument date rows
  bucketed by exact calendar arithmetic, adopted only on exact identity closure; Proact
  312,458 / 166,153, check failed → passed). Numbers taken from each lane's own evidence file.
- **Desktop section**: already reads **0.3.2** built from `e216fd4` with both artifact names — v071's
  own edit (its WO allowed the desktop sentence); confirmed against `v071.md` §1/§3, not changed.
- **Hardening-loop intro refreshed**: the stale "four hardening-loop rows" count replaced with the
  overview — seven rounds (seeds 1–7: v035, v037, v041, v048, v051, v063, v072 — the seed-3/4 rounds
  have evidence but no table row), 67 company draws, four mechanism families (bucket rows
  `_between_rows` / bucket columns `_fill_bucket_columns` / per-instrument dates
  `_date_bucket_derive` / stated-zero prose `zero_if_stated`), with `scripts/replay_check.py` and
  `eval/run.py --stored-kb` named as the loop's regression and scoring tools. The 67: seeds 1–4 =
  40 draws / 34 distinct (v057), seeds 5–6 = 10 + 10 (v069's replay split 162 = 102 `data/kb` + 60
  seed-KB), seed 7 = 7 (v072) → 40 + 10 + 10 + 7 = 67.
- Checks: README renders as one row per lane with evidence links (table columns intact); owner-
  identifier grep over the full diff vs. baseline — empty; no code, schema, `data/` or `eval/` file
  touched, so the build/lint/test gates are not exercised by this lane (docs-only diff, verified via
  `git diff --stat`).
