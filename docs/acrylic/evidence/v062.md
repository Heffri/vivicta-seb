# v062 — `zero_if_stated.subject_terms`: bare-"loan(s)" no-debt prose now proves a 0 (Vicore Pharma, BioGaia) — zero model calls, red/green + seed1–5 replay

Lane `ao/vivicta-glm-20/v062-stated-zero-vocab`, off `origin/acrylic` (tip `85c9704` at dispatch and
at delivery). **Zero real inference**: every observation below is deterministic post-processing
(`extract()`) fed constructed or stored answers; page texts come read-only from the shared seed KBs.

## Why (v051's finding, reproduced offline first)

v051 (seed 5) recorded two live no-debt statements `_stated_zero` declines, because its subject gate
(e) reads only schema `keywords` ∪ field `synonyms` and both sentences carry only the bare word:

- **Vicore Pharma Holding, p.46** (real seed5 text): *"Currently, the group is financed by
  shareholders' equity and is therefore not exposed to risks related to external loan financing."*
- **BioGaia, p.153** (real seed5 text): *"The group has no external loans."*

**Red observation** (test added first, unmodified tree): both constructed answers (`total_debt=0`,
the sentence as quote, buckets null) drop — `value=None`, warning `total_debt: 0 is printed on none
of pages [1]; dropped as computed, not read` (Vicore shown; BioGaia the same shape), exactly the
live-run behaviour. Gates (c) digit-free, (d) verbatim-on-page and (f) negation already pass for both
sentences; only (e) fails.

## The change (minimum, both files)

- `backend/schemas/debt_maturity.json`: one new key — `total_debt.zero_if_stated.subject_terms:
  ["loan", "loans"]`. Synonyms / keywords / negations untouched; nothing else in the file touched.
- `backend/pipeline/extract.py` (`_stated_zero` only): the subject-vocabulary list gains
  `sf["zero_if_stated"].get("subject_terms", [])`; docstring updated. The other four gates (opt-in,
  value exactly 0, digit-free quote, verbatim on cited page, negation word) byte-identical.

**Green observation**: same two inputs → `total_debt 0 @ 0.5`, evidence contains `stated_zero`,
identity `maturity_sums_to_total` **passed** (`0 (due_within_1_year null) + 0 (due_1_to_5_years
null) + 0 (due_after_5_years null) == 0`), same tier as Creades v050. No new confidence rule — the
0.5 comes from the existing cap arithmetic.

## `subject_terms` — why exactly these two (appearance scan over seed1–5 page texts)

Every candidate from the work order's list **does** appear in seed1–5; the scan therefore decided on
what the appearances *are* (substring/standalone page counts across all five seed KBs):

| term | substring pages | standalone pages | verdict |
|---|---|---|---|
| `loan` | 501 | 259 | **in** — Vicore p46 (the gap), Apotea seed1 p102 ("…partnership agreements, or loans"), p113 ("portion of the loan that does not have an unconditional right…") |
| `loans` | 360 | 360 | **in** — BioGaia p153 (the gap), Apotea seed1 p91/p100; kept beside `loan` so the list stays meaningful if matching ever tightens to word boundaries |
| `lån` | 816 | 84 | out — standalone uses are lending/collateral prose: Knowit p133 *"knowit har inte lämnat lån…"* (lent, not borrowed), Alligo p136 *"inga varor har ställts som säkerhet för lån…"*; Swedish no-debt prose uses the already-covered "räntebärande skulder"/"upplåning" |
| `borrowing` | 300 | 130 | out — standalone uses name other subjects: Ework p56 "marginal borrowing rate", Cloetta p179 "borrowing limits" |
| `borrowings` | 198 | 198 | out (redundant) — already a schema `keyword`; the union contains it regardless |
| `debt` | 439 | 399 | out — pulls in net-debt and asset-side prose: Bico p140 *"contingent considerations are not included in the net debt measure"*, Storytel p81 / Catella p104 "debt instruments" (financial **assets**); a "no net debt" sentence is not a gross-borrowings 0 |
| `skulder` | 292 | 249 | out — standalone uses are related-party / assets-and-liabilities prose (Xano p90 *"fordringar på och skulder till närstående…"*); as a substring it also lives inside "leverantörsskulder" |
| `upplåning` | 42 | 35 | out (redundant) — already a schema `keyword` |
| `credit facility` / `kreditfacilitet` | 85 / 26 | 85 / 15 | out — facility-utilization prose (Boozt p117, Knowit p35/p135, Academedia p86), not no-debt statements; the one seed report whose facility sentence is famous (Svolder) carries digits and belongs to the old path |

Including the "out" rows would mint false zeros on real corpus sentences (Knowit p133, Bico p140,
Xano p90 are digit-free + negation-bearing today) for zero known benefit — no seed company's
no-debt sentence needs them.

## Counter-examples (all green in `pipeline.test_confidence`)

v050's six all still drop / behave byte-identically (off-page sentence, wrong-subject sentence —
"Creades klassificering … tillgångar och skulder" still has no negation word, bucket field opt-out,
digit-bearing Svolder quote, bare XANO row label, Flat Capital). New this lane:

| # | Case | Outcome |
|---|---|---|
| 7 | bare-`loan` negation sentence **not printed** on the cited page ("The group has no bank loans whatsoever.") | dropped as computed, not read — subject terms buy no licence beyond the page |
| 8 | *"…no loans to associates."* verbatim on the page (lending, an asset — says nothing about borrowings) | **accepted 0 @ 0.5 — known boundary, pinned on purpose.** Word-level subject matching cannot see the "to associates" complement; blocking this shape needs phrase-level context, a different mechanism. Reported honestly per the work order, not forced away. |

## Replay — 0 CHANGED everywhere (stored-answer argument holds, as the work order expected)

`python scripts/replay_check.py --baseline origin/acrylic`, stored fields fed back as the model
answer, zero calls. Full reports: `v062/replay-debt.txt`, `v062/replay-income.txt`,
`v062/replay-datakb.txt` (header paths masked after generation).

- **debt_maturity, seed1–5 KBs: 50 companies, 50 same, 0 CHANGED** (7 not replayable: stored runs
  with no derivable candidate pages — all-null fields, nothing to move).
- income_statement on `data/kb`: **101 companies, 101 same, 0 CHANGED** (the seed KBs carry no
  stored income extractions — replay-income.txt records that; default-KB run covers it).
- debt_maturity on `data/kb`: 1/1 same.

Why Vicore/BioGaia themselves don't flip on replay, stated plainly: their stored reads are `0@0.25`
kept by the old **printed-zero-nearby** path (Vicore p46 prints "SEK 0 thousand"), and a page with a
printed 0 never reaches `_stated_zero` — identical on both sides. A stored field that was *dropped*
replays as null on both sides (raw answers are not stored). So the replay proves the change cannot
move anything on disk; the positive flip is the constructed-answer red/green above, the same two-part
argument v050 used for Creades.

## Gates

- `python -m pipeline.test_confidence`: red (new cases fail on unmodified tree, observation above)
  → **green** with the two edits, v050 block untouched and passing.
- `python scripts/label_regression.py --schema debt_maturity --schema income_statement --baseline
  origin/acrylic`: **identical=739, changed=0**, skipped=173 (synonyms untouched, as expected).
- `pipeline.test_parse` / `test_kb` / `test_paths` / `test_llm`: all green.
- Owner-identifier grep over every changed/new file: empty. (`data/kb` untouched; working tree
  carries only the four territory files plus this evidence directory.)

## Deliberately not done

- No model calls, no negation-list changes, no bucket semantics, no `_check` / bucket-column /
  parse.py / locate.py / frontend / `data/kb` changes.
- The loans-to-associates boundary (#8) left open by design — see the counter-example table.
