# w207 — demo-day freeze: version lock, accuracy framing, switch checklist, script names

Docs-only lane, no code touched. Territory: `README.md`, `docs/DEMO.md`, this file. Baseline
`origin/acrylic` @ `0dff6d1` (2026-09-22), which already contains m01 (source-evidence fixes) and
m02 (Sebastijan's workspace shell, `EXTRACT_SCAN_ALL` as an offline tool) merged in.

## 1. Version lock

`docs/DEMO.md`'s "Branch / commit demoed" row still named the pre-m02 commit `fca5051` with a
hedge to re-derive the real sha. Replaced with the actual current `origin/acrylic` head,
`0dff6d1` (2026-09-22), described as the m01+m02 merge, keeping a re-fetch instruction only for
"if demoing later than this freeze."

## 2. Accuracy framing (README.md `## Accuracy`, DEMO.md `## What we cannot promise`)

| Item | Finding | Action |
|---|---|---|
| Stored-library 89.1% / 84.0% | Already correct in both files | No edit |
| Held-out round 2 = 36/40 (90.0%), 14/17 (82.4%), "current benchmark", n=10 not extrapolated | Already stated correctly ("is the current held-out sample" / "Neither n=10 measurement is a market-accuracy claim") | No edit |
| Round 1 = 65%/47%, retired to tuning | Fraction and "no longer the current held-out benchmark" already present; bare percentages were not | Added `(65.0%)` / `(47.1%)` next to the existing 26/40 / 8/17, matching how round 2 is already presented |
| Labelled-page coverage 268/276 = 97.1% | **Stale** — README still read locator 263/276 → sweep **264/276 (95.7%)**, citing only w198. w203 (already merged into acrylic) took the full-text-sweep supplemental hits from 1/276 to 5/276, i.e. 268/276 (97.1%), and documented the remaining 8 as named structural table-layout gaps | Fixed: README now reads 268/276 (97.1%), cites w198 (264/276) and w203 (268/276) in sequence |
| Debt section alone 231/271 (85.2%) / 171/221 (77.4%) | Already present | No edit |
| Error nature 34 empty / 6 non-empty-wrong | Already present | No edit |
| "What we've tried": Laya 79.6% < naive 80.6% (69.9% calibrated); Docling ~271× slower (1710s vs 6.3s) | **Absent** — no "what we tried" section existed anywhere in README | Added README Accuracy item **6) What we've tried and ruled out**, numbers copied verbatim from `docs/acrylic/evidence/m02.md`, linked there |
| DEMO.md "What we cannot promise" | Reviewed in full: "89.1% is the curated library's score, not a first-pass or market rate" is already accurate and consistent with the README edits above | No edit |

## 3. Default-switch checklist item (fable's c002 ruling: all three off for demo day)

Added a dedicated "Extraction switches off for demo day" bullet to DEMO.md's hardening checklist
(`EXTRACT_SECOND_PASS` unset, `EXTRACT_SCAN_ALL` unset, `EXTRACT_MERGE_RUNS=off`) with a
fact-checked "how to confirm" clause rather than a generic one:

- Read `backend/app.py`: `GET /api/config` (line 1181-1190) echoes `merge_runs` (from
  `merge.mode()`, i.e. `EXTRACT_MERGE_RUNS`) — confirmable live.
- `EXTRACT_SECOND_PASS` (read at line 754) and `EXTRACT_SCAN_ALL` (read at line 1274, only inside
  `extraction_identity`'s cache-key fingerprint) are **not** part of `/api/config`'s response.
- Grepped `frontend/src/components/SettingsView.tsx`: it renders a "two-pass" status
  (`EXTRACT_TWO_PASS` — a third, older, unrelated switch) and a "merge" status
  (`EXTRACT_MERGE_RUNS`); it has no element for `EXTRACT_SECOND_PASS` or `EXTRACT_SCAN_ALL` at all.

So the checklist item says plainly: `EXTRACT_MERGE_RUNS` is checkable via Settings or
`/api/config`; the other two have no UI/API surface by design and must be confirmed by checking
the demo machine's own shell environment instead — the doc does not claim a UI element that does
not exist.

## 4. Hardening checklist, item by item

| # | Work order's concern | What the current checklist actually said | Action |
|---|---|---|---|
| a | OCR language packs now ship with the app (w204); the old "run `setup_ocr.py` on the demo machine" line should be gone | Already reads "`desktop/scripts/prepare-resources.js` … A clean installed app does not need repository scripts" — no `setup_ocr.py` mention anywhere in the file (checked with a full-file grep) | Verified correct, no edit |
| b | Fixed ports (w201) mean collection/tone now survive a restart; the old "if you restart on stage you must redo Collection: All" caution should change | No such warning is present in the current file (full-file grep for restart/reopen/relaunch found nothing matching that claim) — likely already removed by an earlier lane | No warning to remove; instead added a positive, verified sentence to the existing "Collection on stage" bullet: stable ports mean the collection and tone choice survive an in-demo restart |
| c | Private-roster members (w202) need a line of talk track | Absent | Added new bullet: say up front that the company has no standalone report and the app goes straight to the parent's page, using the real Sarnova → Investor AB p.41 example, zero AI search triggered |
| d | Search now has a visible trace (v194); worth a line | Absent | Added new bullet describing the live stage-by-stage trail and that it survives a tab switch, framed as encore-only |

## 5. Three-minute script entry names

Cross-checked every bolded UI name in the 0:00–3:00 script against the current source (stronger
evidence than the m02 screenshots alone, since none of the 5 `docs/acrylic/evidence/m02/*.png`
captures happens to frame the Results sub-tab strip — read the source for the literal label
strings instead, then treated the screenshots as corroborating, not primary, evidence):

- `frontend/src/components/shell/tabs.ts`: rail labels are exactly `Extract`, `Results`,
  `Compare`, `Ask`, `Review`, `Knowledge base`, `Company map`, `Settings` — matches every rail
  name the script uses.
- `frontend/src/components/ResultsView.tsx:188-197`: sub-tab labels are exactly `Maturity
  profile`, `Checks & review`, `Export` — matches the script's "open **Maturity profile**", "open
  **Checks & review**" (both the 0:20 and 1:00 steps), and "**Export** →".
- `frontend/src/components/ResultsView.tsx:176`: `` `Review ${selected.label}` `` — matches "open
  **Review <figure>** under the table".
- `frontend/src/components/UploadView.tsx:241`: button text "Open a real debt sample · saved
  result, zero model calls" — the script's "**Open a real debt sample**" is a correct (truncated)
  prefix of the real button text, not a stale name.
- `frontend/src/components/ResultsView.tsx:200-202`: buttons are literally "Export JSON",
  "Export CSV", "Export PPTX" — matches.
- `frontend/src/components/KbView.tsx:232`: literal "Open" button — matches "search \"karnell\" →
  **Open**".

Zero mismatches. All five timed steps' entry names were already correct going into this lane —
consistent with `consult-merge.md` §B.6 listing the same four line-level fixes as work the m02
lane would apply on its own delivery (`git log -- docs/DEMO.md` shows m02's own commits
`71e8283`/`822fdee` already touched this file). **No edits made to the three-minute script's step
text or names.**

## Gates

- `frontend/npm ci` (fresh install — `node_modules` did not exist in this worktree yet), then
  `npm run build`: green, only the pre-existing >500 kB chunk-size advisory.
- `npm run lint` (oxlint): exit 0, the same 13 pre-existing advisory warnings every other current
  lane's evidence records (`only-export-components` × 9, `set-state-in-effect` × 4) — no new
  warnings, since no `.ts`/`.tsx` file was touched.
- `git diff --stat` against the pre-edit tree: `README.md`, `docs/DEMO.md` only.
- `data/kb`: untouched — this lane never started the app (`git status data/kb` empty).
- Owner-identifier gate: run as its own separate command before push, result recorded in the
  delivery message (LESSONS 32/33/45 — never combined with the push itself).

## What this lane did not do

- Did not touch the "Measured rehearsal" table or any row of the Version-lock table besides
  "Branch / commit demoed" — that section is w206's territory; at dispatch time
  `git branch -r` showed no `w206` branch on origin yet, so there was nothing to merge. Re-checked
  immediately before push (recorded in the delivery message).
- Did not start the app, run the model, or take new screenshots — §5 only required reading the
  existing m02 screenshots and the current source, which was sufficient to verify every label.
- Did not touch code, `data/kb`, or any file outside `README.md` / `docs/DEMO.md` / this file.
