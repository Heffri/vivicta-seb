# v056 — docs backfill (v034/v049/v050/v052/v053/v054/v055/v059) + three small fixes (retrieval-mode status strips, stale desktop comment, SetSettingsResult type lag) — evidence

Lane `ao/vivicta-glm-19/v056-docs-and-small-fixes`, off `origin/acrylic` at its dispatch tip `402f226`
(HEAD == baseline at start; re-fetched at delivery — `origin/acrylic` had moved to `653d4a4`, v057's
eval-labels lane touching `eval/*` + its own evidence only, zero overlap with this lane's files;
merged in, gates re-run on the merged state). Docs + display-field swaps only:
no logic, no styles, no backend code, no schemas, no `data/` content. **Zero model calls** — the one
backend run below served `/api/config` to a browser tab and nothing else. Per the work order's "不做"
the README row for v057 (merged mid-lane) is left for the supervisor, like v051/v058.

## What changed

**`docs/acrylic/README.md`**

- Seven rows appended to the "Backend hardening" table, one per lane, numbers taken from each lane's
  own evidence: v034 (three-state retrieval; offline eval hit@3 41.4%→**87.0%** income / 59.7%→**98.7%**
  debt), v049 (column-aware parse, `PARSER_VERSION` 3→4; split-row rate 0.27%→**0.20%** debt /
  0.36%→**0.29%** income over 35 PDFs, `data/kb` quotes 8/8 kept, pymupdf pin), v050 (`zero_if_stated`;
  42/42 replay, one-company blast radius), v052 (row-anchored year header + Group-side rule;
  135-company replay, 2 changed entries = the intended medcap_2025 fix), v053 (`replay_check.py`;
  red control reproduces v048's 2 recorded changes), v054 (`EXTRACT_QUOTE_RETRY` default off;
  9-company Codex run, 1 trigger, 0 adoptions), v059 (bm25 zero-hit short-circuit, model not called).
- Desktop section: the packaged release is now stated as **0.3.0**, built from `d1777d9`, artifact
  names `Annual Report Parser-0.3.0-portable.exe` / `Annual Report Parser-Setup-0.3.0.exe` (~141.4 /
  ~141.6 MB), per v055 §7.
- "Assumptions to confirm with Kristian": two bullets added — prose no-debt sentence proves
  `total_debt = 0` ([v050](v050.md)); repeated Group | Parent year header resolves to the Group side
  ([v052](v052.md)). Intro line widened from "Three calls" to cover the switch/code-gate assumptions
  (it already listed four bullets).

**`README.md` (root, Run it only)**

- The `pip install -r requirements.txt` line now notes `pymupdf` is pinned to `1.27.2.3` with a
  pointer at `docs/acrylic/evidence/v049.md`.
- One stale sentence fixed in the same section: "Ask's retrieval still needs `LLM_BASE_URL`" was
  pre-v034 text — since v034 a codex/claude setup without a base URL gets real BM25 Ask, and a base
  URL upgrades retrieval to hybrid. (Judgment call, inside the Run it section; flagged here for the
  supervisor.)

**`backend/README.md`** — read, not touched: v034's three-state paragraph and v054's quote-retry
section do not contradict each other (the embeddings paragraphs in both CLI sections already state
the bm25 fallback).

**`frontend/src/components/SettingsView.tsx`** — `StatusRow`'s third segment swaps on
`status.retrieval === 'bm25'` (v034's codex/claude-no-base-URL state): it reads `retrieval BM25`
instead of naming an embed model that is not in use. Hybrid keeps the embed model name; fixture mode
unchanged (`retrieval: 'fixture'` falls to the old branch); an absent `retrieval` (older payload)
also falls to the old branch. No layout or style change — same `<span>` shape, one conditional.

**`frontend/src/components/shell/StatusBar.tsx`** — the same swap for the shell footer
(`gpt-5.6-terra · retrieval BM25` in bm25 mode). v034 changed only this line's hover title; the
visible text now matches it. Same conditional shape, no style change.

**`frontend/src/main.tsx`** (types only) — `SetSettingsResult`'s ok-branch `config` field was an
inline literal missing `retrieval`; it now reuses `Config` from `api.ts` (the work order's preferred
option; no import cycle — `api.ts` imports `./types` only). This is accurate at runtime:
`desktop/main.js`'s `applySettings()` resolves that field from `/api/config`'s own JSON, which has
carried `retrieval` since v034. Known leftover, outside this lane's file list: `api.ts`'s comment
above `Config` still says main.tsx "predates" the field — now true only historically; behavior is
unchanged because `retrieval` stays optional in `Config` itself.

**`desktop/settings.js`** — the one stale comment v055 §4 flagged (codex case: "`/index` and `/ask`
still gate on `LLM_BASE_URL` alone, so Ask stays unavailable without it") now states the current
behavior: the gates are `_llm_configured()`, and Ask without a base URL retrieves pure BM25 (hybrid
with one). The code below it never needed the fix — comment-only change.

## Before → after observation

Before (red): the pre-change `StatusRow` rendered `embed {status.embed_model}` unconditionally —
v047's own committed screenshot of this exact flow (codex card, Save, no base URL,
`evidence/v047/settings-codex-saved.png`) shows `provider codex model gpt-5.6-terra embed bge-m3
two-pass on`, and v059's "Not done" records the type lag and the unswapped strips as flagged gaps.
After (green): both shots below, same flow, real backend.

## Screenshots

Backend started with `LLM_PROVIDER=codex`, no `LLM_BASE_URL`, on port 8019 — `GET /api/config`
returned `{"model":"gpt-5.6-terra","embed_model":"bge-m3","base_url":null,"llm":true,"provider":
"codex","retrieval":"bm25"}`. Frontend via the shared `vite.lane.config.mts` proxy on port 5219.
Driven in Edge (Playwright, `chromium.launch({ channel: 'msedge' })`) with only `window.arp.settings`
stubbed at the IPC boundary — v047's documented approach, because the save flow is desktop-only and
the shared `shot.mjs` DSL has no init-script hook; the stub's `set()` returns the *real* `/api/config`
payload, exactly what `desktop/main.js` resolves. No `/extract`, no `/ask`, no CLI invocation — zero
inference. Both fixed surfaces are visible in each shot (Settings strip mid-page, shell footer at the
bottom):

| View | File |
|---|---|
| Codex card, after Save, dark tone | `v056/settings-codex-saved-dark.png` |
| Codex card, after Save, light tone | `v056/settings-codex-saved-light.png` |

Both show `Running now  provider codex  model gpt-5.6-terra  retrieval BM25  two-pass on` and the
footer `gpt-5.6-terra · retrieval BM25`; the embed model name is gone from both in this state.

## Gates

`cd frontend && npm ci && npm run build` (tsc -b + vite) exit 0; `npm run lint` (oxlint) exit 0 —
warnings only, all the pre-existing `react(only-export-components)` set (ResultsView/MaturityChart/ui/*),
none in the touched files. Backend venv installed from the pinned `requirements.txt`
(`PyMuPDF 1.27.2.3` confirmed live). `git status --short data/`: empty (only `/api/config` was ever
called). Processes started by this lane (uvicorn 8019, vite 5219, one Edge instance per shot) stopped
by tracked PID; port scan clean afterwards.

## Territory check

`git diff --name-only` vs baseline = `README.md`, `desktop/settings.js`, `docs/acrylic/README.md`,
`frontend/src/components/SettingsView.tsx`, `frontend/src/components/shell/StatusBar.tsx`,
`frontend/src/main.tsx`, plus this file and `v056/*.png`. No backend code, no schemas, no scripts, no
other frontend files, no styles. Owner-identifier grep over every changed/new file: empty. Lane temp
files (screenshot driver) under the lane's own `uaw-lanes` directory only.
