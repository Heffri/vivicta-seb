# v176 — refreshed packaged-app closed loop

Branch `ao/vivicta-seb-96/v176-app-refresh-closed-loop`, based on
`origin/acrylic` at `fba373d`; the packaged build is merge commit `25ed26b`.
Territory: the shared unpacked test package plus
`docs/DEMO.md`, this evidence and its screenshots. No product code, API contract, evaluation data
or model provider was changed or called.

## Package freshness — red, then green

Before the work, the shared test package's `BUILD.txt` named `8a13271`, not the current acrylic
tip. A process check found no package process. One direct invocation of the refresh helper was
rejected because its own parent command line matched the helper's conservative running-app scan;
the retry used a small external wrapper, so the scan could only see real package processes.

The first full refresh built and staged all four resources from the then-current acrylic source.
After merging the current acrylic tip, a final warm full refresh, captured to a local-only log,
completed in **41 s** and reported these replacements:
`frontend-dist`, `backend`, `data`, and `app.asar`. The resulting `BUILD.txt` names
`25ed26b51afcdbbd32156688ed6b2143fe2c14f6` on this branch. No package process was running after
the final close.

## Clean-userData closed loop

The packaged executable was launched with a newly created, quoted `--user-data-dir` value. It
answered `/api/kb` in **18.4 s** on first start, with **206** reports. Its first sync recorded
`+206 kb entries, +4 files`.

| Action | Measured time | Observed result |
|---|---:|---|
| Landing page → **Open a real debt sample** | 0.28 s | Karnell Group's saved debt result opened without an extraction/model call. |
| Select Karnell **Due 1–5 years** | 0.07 s | Source panel showed page 106 and its highlighted verbatim quote. |
| KB → All → Ependion | 1.48 s | `Due after 5 years` visibly reads **Not printed in this report**. |
| Confirm Ependion total borrowings | 0.71 s | Human-confirmed review saved. |
| KB Export all CSV | 1.24 s | 26,511 bytes and **105** data rows. |
| KB Export all PPTX deck | 5.75 s | 494,804 bytes and **106** slides (summary + 105 reports). |
| Compare Ependion + Karnell → fully loaded upcoming-maturities wall | 1.23 s | All-collection wall rendered with 0/105 comparable, 105 basis-unconfirmed, 13 missing totals and 49 missing `<1y`. |
| Graceful close | 0.27 s | Owned launcher and backend exited. |
| Measured reopen → `/api/kb` | 4.9 s | 206 reports; saved review still returned `confirmed`. |

The two reopens each logged `+0 kb entries, +0 files, 1 kept (user-modified)`: the kept file is
the confirmed review, so the `1 kept` is evidence of preservation rather than a bundle update.
Screenshots were captured through a browser pointed at the packaged backend's same-origin frontend,
not from a full desktop capture:

- `v176/karnell-source-quote.png` — selected bucket, page 106 and highlighted quote.
- `v176/ependion-not-printed-review.png` — explicit absence plus saved human confirmation.
- `v176/compare-upcoming-maturities.png` — populated all-collection upcoming-maturities wall.

### Non-blocking exception

Each unpacked-package start logged that the background updater could not find `app-update.yml`
(`ENOENT`). The app still returned `/api/kb`, loaded records, preserved the review, and completed
both exports. This is recorded, not fixed: this lane may not change package or desktop code, and
the shared refresh target is not an installer/update-feed artifact.

## Documentation and numerical check

`docs/DEMO.md` now has the measured **实测** section and changes the first demonstration step to
the actual first-screen **Open a real debt sample** control, retaining the KB route as the manual
alternative. The rest of the rehearsal matched the script.

`data/kb` contains **206** directories. Offline `python eval/run.py --stored-kb data/kb --no-fail`
reported values **327/367 (89.1%)** and cited pages **263/313 (84.0%)**. Those values already match
both `README.md` and `docs/acrylic/README.md`, so neither numerical claim was changed.

## Gates

- `frontend/npm run build` — pass (the existing large-chunk advisory remained non-fatal).
- `frontend/npm run lint` — pass, with the pre-existing 11 warnings.
- `desktop/node packaging.test.js` — pass: 4 shell modules listed.
- `python eval/run.py --stored-kb data/kb --no-fail` — completed with the numerical results above.

No tracked `data/` file changed during the packaged-app exercise.
