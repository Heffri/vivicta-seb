# v159 — README rows v146–v158 and current accuracy/provider guidance

## Scope

Documentation only: `docs/acrylic/README.md` and this evidence file.  I read the required lane
rules and the Summary/Conclusion material for v146, v148–v152 and v154–v158.  The required final
`origin/acrylic` refresh added v158, so its conclusion is included as a row.  The UAW demo tree was
also inspected before editing: `src/workbench-shell/renderer/themes/theme-acrylic.css` and the
source scan contain no reusable README change table or model-provider guidance, so no UAW code or
prose was copied.

## Red → green

- **Red:** before this edit, the hardening table linked none of v146, v148–v152 or v154–v158, and
  the Data/Accuracy bullet still reported 310/367 values and 249/311 pages.
- **Green:** one chronological table row now links each available lane, and the Model providers
  section states the three evidence-backed operating conclusions: terra over sol, union for fresh
  runs versus stored-third-vote majority for reruns/re-publishes, and page-select hints off by
  default.

## Re-run data and checks

On the delivered tree, `data/kb` contains **206** entries, including **105** stored
`debt_maturity` extractions.  `python eval/run.py --stored-kb data/kb` reports values
**317/367 (86.4%)** and cited pages **255/313 (81.5%)**.  The README cites those observed numbers,
not a projected code result; this lane made no model or network call and changed no `data/` file.

`git diff --check` is clean.  The required frontend build passed; lint exited zero with the
repository's existing 10 warnings.  The final release scan over this lane's changed paths was
clean before publication.
