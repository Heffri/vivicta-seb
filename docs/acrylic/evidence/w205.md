# w205 — README rows for v190–w203

Docs-only: `docs/acrylic/README.md` is the only tracked file changed besides this evidence. No
`backend/`, `frontend/`, `schemas/`, `eval/` or `data/` byte touched, zero model calls, no servers
started. Baseline `origin/acrylic` `b9f3c65` (re-fetched at delivery — unchanged; w203/w195/w202
already present).

## Rows added to `docs/acrylic/README.md` (13 lanes), by section

- **What a colleague sees now** (top summary, 1 new bullet — the round-2 held-out sentence for
  [v190](v190.md) was already present from an earlier lane, verified unchanged): a new
  **Label-page coverage** bullet states `scripts/coverage_check.py`'s combined number, locator
  263/276 (95.3%) → w198's sweep 264/276 (95.7%) → w203's wordlist fixes **268/276 (97.1%)**,
  naming the 8 remaining structural misses ([w198](w198.md), [w203](w203.md)).
- **Frontend table** (4 rows, appended after the v187 merged-tree-e2e row, numeric order):
  [v191](v191.md) (Extract — bounded OCR on registration, page-budget 422 with a "Run OCR anyway"
  resend), [v194](v194.md) (Extract — AI company search job trace, state survives tab switches,
  retry), [w200](w200.md) (Extract — AI company search prefers a saved copy over a web/model call,
  "Search the web anyway?"), [w202](w202.md) (KB / Company map — the 13 private-roster members
  open their parent report directly instead of risking a mis-accepted standalone PDF).
- **Backend hardening table** (8 rows, appended after the v189 iXBRL-probe row, numeric order):
  [v193](v193.md) (atomic PDF publication + a malformed-reply 500 fixed; a reported packaged "flash
  exit" did not reproduce, but the audit's three real fault paths — incl. a desktop
  `render-process-gone` crash-recovery handler — are fixed regardless), [w196](w196.md) (US 10-K
  net-income reachability, three independent root causes: a locator exclusion false-positive, an
  Intel doubled-text-layer parser defect, Ask's page-window cutting a statement in half),
  [w197](w197.md) (bounded required-null second pass, `EXTRACT_SECOND_PASS`, default off),
  [w198](w198.md) (zero-model full-text field sweep feeding the second pass; coverage 264/276,
  95.7%), [w199](w199.md) (KB "Building…" poll-storm mechanism fixed on both the lock-probing
  backend and the frontend's backoff), [w201](w201.md) (packaged app tries a stable loopback port
  range so origin-scoped `localStorage` prefs survive restarts), [w203](w203.md) (12 last-mile
  coverage gaps read on their real pages; 4 close via wordlist, 8 named as structural; combined
  268/276, 97.1%), [w195](w195-qa.md) (demo-day QA sweep over the packaged app — 26/32 ok, 2 broken
  incl. the w199 poll storm this batch fixes, 3 need a real model, 1 not CDP-automatable).
- **How to verify**: `pipeline.test_jobs` (new in v194) added to the backend command list; suite
  count **15 → 16** (13 under `pipeline/`); the e2e comment **59 → 66 tests** (w202's post-merge
  gate number, the latest recorded).

## Numbering note

v190 and w195 both get one table/summary mention each as instructed, not a duplicate: v190 (a
blind eval round with no code/schema/pipeline change) only ever earns the top-summary sentence,
matching the existing precedent for v178 (no Backend-hardening-table row either); w195 (test-only
QA sweep, no product code) gets a Backend-hardening-table row, matching the precedent set by
v142's packaged-app acceptance walkthrough. Evidence file names: `w195-qa.md` (not `w195.md`,
confirmed against the actual filename in `evidence/`), all others `vNNN.md`/`wNNN.md`.

## Gates

- `git diff --name-only origin/acrylic` → exactly the two files above.
- Owner-identifier grep over the changed files (run as its own command, before any push) → clean.
