# v098 — README rows for v088–v094 + the 09-18 merge section

Lane `ao/vivicta-glm-39/v098-docs-rows`, off `origin/acrylic` at `05bc233`. Docs-only lane: zero model
calls, no code, schema or `data/` touched, `npm run build` not run. Every number below is quoted from
the named lanes' own evidence files and the merge commit's message — nothing re-measured.

## What changed, by section

- **`docs/acrylic/README.md`**
  - *Frontend table*: +2 rows — v089's Settings "Maturity basis" select and the basis display on the
    status strip / Results header; v092's KB open-without-a-cached-PDF behavior and which of its
    frontend pieces survived the 09-18 merge.
  - *Backend hardening table*: +6 rows — v088 (seed 9: 0 full / 7 partial / 3 failed, zero schema
    commits, four rejected hypotheses, the Karnell/Rusta levers dispatched as v095/v096), v089
    (`DEBT_BASIS`, the basis-key rename to `maturity_basis`, Instalco closing under `undiscounted`),
    v090 (18 Financials, four shapes, the bank-borrowings ruling in the schema description,
    `random_check.py --sector`), v092 (with the supersession note), v093 (`scripts/publish_kb.py`,
    72 + 12 = 186 companies in `data/kb`, 85 with debt, the `--stored-kb data/kb` numbers), v094
    (91 rows / 35 companies, its two scoring tables condensed).
  - *Hardening summary paragraph*: "Eight rounds, seeds 1–8, 77 draws" → "Ten rounds — seeds 1–9 plus
    a Financials sweep — 105 draws", link list extended with v088/v090.
  - *New section* "2026-09-18: merged with Sebastijan's demo/main" (≤15 lines): what he pushed, how
    the conflicts resolved (`basis` → `maturity_basis`, v092 superseded by
    `saved_report_id`/`require_pdf`), and the two semantic changes — `require_explicit_values: true`
    (empty buckets no longer counted as 0; replay of the 68 replayable `data/kb` debt entries moves
    21 check/confidence results, MedCap `due_within_1_year` 102.3 → null) and the KB page defaulting
    to the Wallenberg collection filter (the 85 Mid Cap debt entries stay visible to Ask and eval).
  - *Assumptions*: #1 annotated with the v089 user option (default unchanged); #3 annotated "team
    default flipped 09-18, one schema flag"; bank-borrowings wording added, copied from v090's
    issue-#1 item 5.
  - *Contract gaps*: the v092-fixed bullet gains the supersession parenthetical.
- **`backend/README.md`**: new section "Publishing hardening results into `data/kb`
  (`scripts/publish_kb.py`) — v093" (invocation, best-run-per-stem, replay gate, idempotence, the
  current 186/85 counts, how to fold in a future seed); the `DEBT_BASIS` section's extraction field
  name corrected `basis` → `maturity_basis` (the merge rename — v089's text predated it).
- **`eval/README.md`**: one sentence — `labels.csv` now holds 181 hand-verified rows across 62
  companies (v057 + v094).

## Not done / out of scope

- The desktop-release paragraph (0.3.4) is untouched — the merge brought CI auto-update builds, but
  describing/repackaging them was nobody's instruction to this lane.
- No verification runs (nothing ran); no screenshots (nothing visual changed).

## Owner-identifier check

`git diff --name-only origin/acrylic` over the four files this lane touches, grepped for the owner
identifier: empty.

## Delivered

Branch `ao/vivicta-glm-39/v098-docs-rows`, pushed to `origin` (the local hub) only.
