# w217 — every number in the external docs re-run against the repo (zero inference, zero downloads)

Docs-audit lane. Baseline `origin/acrylic` @ `dfce54f`; branch `ao/vivicta-glm-109/w217-docs-numbers`.
Territory: the nine externally-readable docs (`README.md`, `docs/ACCURACY.md`, `docs/acrylic/README.md`,
`backend/README.md`, `eval/README.md`, `docs/PERFORMANCE.md`, `docs/API.md`, `frontend/README.md`,
`desktop/README.md`). No model calls, no PDF downloads, `data/` untouched except reads; the backend was
started once (fixture mode) only to serve the e2e suite.

## 1. Runnable commands, real outputs

| command (repo root) | output | docs said | match |
|---|---|---|---|
| `python eval/run.py --stored-kb data/kb --no-fail` | values **328/368 (89.1%)**, pages **263/313 (84.0%)**, mean conf 0.75/0.11 | 328/368 (89.1%) · 263/313 (84.0%) | yes |
| `python scripts/eval_breakdown.py --stored-kb data/kb --section debt_maturity` | **232/272 (85.3%)** values, **171/221 (77.4%)** pages, 4 unscored | 232/272 (85.3%) · 171/221 (77.4%) | yes |
| `python scripts/eval_breakdown.py --stored-kb data/kb` (income) | **96/96 (100.0%)** values, **92/92 (100.0%)** pages | 96/96 · 92/92 | yes |
| `python scripts/coverage_check.py` | locator **263/276**, sweep +5, combined **268/276**, 8 uncovered | 263/276 (95.3%) → 268/276 (97.1%) | yes |
| `python scripts/locate_reach.py --section debt_maturity` | in 100 / adjacent 6 / unreachable 0 → **106/106** | (v139 row's 105/105 was that round's snapshot; label file has grown by one company since) | yes, as history |
| `python scripts/locate_reach.py --section income_statement` | **102/102** | 102/102 | yes |
| `python eval/run.py --dry-run` | scores the fixture, exit 1 by design without `--no-fail` | "sanity check" | yes |
| counts (see §2) | evidence **212** md · kb stems **206** · debt **105** · income **102** · labels **375** = 276 debt + 99 income over **118** reports, **106** debt companies · Mid Cap **132** · Wallenberg members **35** · unit tests **21 pass / 0 fail** (node v24, 0.23 s wall) | same | yes |

Unscored label rows confirmed exactly as `eval/README.md` states: 3 `nordic_industrials` income rows
(no KB stem) + 4 `abb_2025` debt rows (abb stores income only) = the 7 that never score, 375 − 7 = 368.

## 2. Number-by-number results (fixed items bold)

- **`docs/ACCURACY.md` §3 — FIXED.** "Of the 40 debt value misses … **34 are empty** … **6 are
  non-empty but wrong**". Today's breakdown: A 28 + D 5 stored nulls = **33 empty**; B 5 + D 2 =
  **7 non-empty wrong** (better_collective 259946, bergman_beving 2038/354, net_insight 0, proact_it 0,
  dynavox 896.3, volati 3246). The number's origin, `v167.md` ("34 output-empty, 6 non-empty mismatched",
  naming its own 6), predates `5bce213` ("data: republish 25 stems through v166 … dynavox total 896.3
  derived from the balance sheet"), which moved dynavox from null to a wrong value. Same fix in
  **`docs/acrylic/README.md`** ("Error nature" bullet).
- **`docs/acrylic/README.md` "How to verify" — FIXED.** `npm run e2e` claimed "**66 tests**". Real run
  (fixture backend + vite dev + Edge, serial, `E2E_BASE_URL` override): **89 passed / 5 failed / 2 skipped
  = 96 total** (the 2 skips are `extract-cached`'s tone pair; this worktree has no
  `data/reports/atlas_copco_2025.pdf`). 66 was the w199-era count; w212's `settings-deep-search.spec.ts`
  and the historical-reports/browser-download specs pushed the suite to 96. `frontend/README.md`'s
  "26 specs … six of them in both tones … seven generate throwaway PDFs" all re-counted: correct.
- Backend suites: 13 under `pipeline/` + 9 at backend root = **22** — matches all three docs; the module
  list in `docs/acrylic/README.md` "How to verify" matches the files one-for-one. `.github/workflows/desktop.yml`
  runs **10** Python modules + `updates.test.js` — matches README. Desktop: 5 `*.test.js` files — matches.
- Constants verified in code: pymupdf pin `1.27.2.3` (+ ~17 % note) · `CHUNK=800` · `BM25_K1,B=1.5,0.75` ·
  hybrid `0.6*c+0.4*b` · `FEWSHOT` default 2 · `EMBED_MODEL` default `bge-m3` · `OCR_LANGUAGE` default
  `eng+swe` · log rotate 5 MB/3 · quote caps 20×8000/24000 · heading ≤80 · questions 2000 · chunks 1–100 ·
  fact threshold 0.7 · OCR cap 0.8 · `OCR_PAGE_BUDGET` 40 · years 1900–2100 · 50 MB upload · dpi 150 ·
  packaged ports 47311–47326 (`length:16` from 47311) · CI version `1.<run_number>.<attempt>`.
- Evidence-cited numbers all match their evidence files: v124 (9→14 of 24), v136 (16/40→19/40, fresh 14/40),
  v160 (23/69→31/69, 17/62→23/62), v178 (26/40 65.0 %, 8/17 47.1 %, 5+9; majority 27/40), v190 (36/40 90.0 %,
  14/17 82.4 %, 1+3, majority identical), w211 (29/40, 12/17, +2.5 calls/+18.4 s), w198 (264/276 95.7 %),
  w203 (268/276 97.1 %), m02 (Laya 79.6/80.6/69.9; Docling 1710 s vs 6.3 s, ~271×), v154 (L0/I2/P7),
  v173 (940→117 ms warm, 8.2→1.8 s cold, 206 stems), v165 (88/105 nulls, 15/105 identities), v176 (41 s
  refresh, 18.4 s first /api/kb, 0.28 s sample, 105-row CSV, 106-slide deck, 4.9 s reopen), v142 (28/28 e2e,
  18 scenarios), w195 (18.4→23 s, 4.9→6–8 s, 105 rows), v185 (209 extractions, 0→78, 2,642→410, 249, 26),
  v003 (34/34). GitHub releases checked read-only: `latest` = `desktop-main`, asset
  `annual-report-parser-main-setup.exe` exists (157.4 MB today vs README's "~165 MB" — moving CI artifact,
  left under the "~"); `desktop-demo` prerelease; 0.3.4 portable/Setup 141.5/141.6 MB as archived rows say.
- `docs/PERFORMANCE.md` is already stamped as the 20 September revision's numbers ("no re-test") and its four
  `docs/benchmarks/*.json` artifacts all exist — left as is.

## 3. Links

Repo-wide relative-link scan (script in the lane dir): **483 links checked, 0 broken**. The scanner's only
two hits are false positives — regex fragments inside backticks in `evidence/v023.md` (`(?:SEK|EUR|…)`) and a
literal `[text](url)` placeholder example in `evidence/v025.md`. No document links to a non-existent file,
including the four benchmark JSONs and the moved `experiments/` layout (`docling/`, `laya/` both present).

## 4. Red finding — code side, reported not fixed

**The KB page's default collection is no longer Wallenberg, and 5 e2e tests fail because of it.**
`4c94188` ("fix: recover historical reports and browser-only downloads") changed `KbView.tsx` line 37 from
`useCollection()` (default `wallenberg`, key `arp-kb-collection`) to `useCollection('all',
'arp-kb-view-collection')`. Consequences on this tree:

- `kb.spec.ts` "collection switch Wallenberg → All → back" (dark + light) fails: first load renders 212
  rows, not the expected 11; `team-workflow.spec.ts` "SEB Mid Cap keeps the KB list within its collection"
  fails ("2 / 2" instead of "1 / 1"); `kb.spec.ts` maturity-wall (dark + light) fails (the wall no longer
  covers only Ericsson). Backend honors the old contract (`/api/kb?collection_name=wallenberg` → 11 rows,
  `all` → 212, `midcap` → 104 — verified live), so the flip is frontend-only.
- Two current-state doc sentences are now false: `docs/acrylic/README.md` "the KB page opens on the
  Wallenberg collection (issue #4)" and "the UI default stays Wallenberg" (v169 row). Not edited here —
  whether the flip is deliberate (the historical-recovery lane also added a "Show all saved reports" banner
  and a new storage key, but updated neither the specs nor the docs) is an owner decision. If deliberate,
  those two sentences plus the five specs need updating together; if not, `KbView.tsx` line 37 reverts.

## 5. What this lane changed (all of it)

1. `docs/ACCURACY.md` — §3: 34 empty / 6 non-empty-wrong → **33 / 7**, with the one-line derivation and
   the dynavox/v166 note.
2. `docs/acrylic/README.md` — "Error nature" bullet: same 34/6 → **33/7** correction (same cause).
3. `docs/acrylic/README.md` — "How to verify": e2e comment "66 tests" → **"96 tests"** and "pass" →
   "suite" (the suite is currently red, §4).

## 6. Notes, no action

- Evidence id ranges have gaps — no `v007`, `v067`, `v070`, `v113`, `v153`, `v161`, `w213`; 190 v + 19 w +
  3 m = the claimed 212 entries, so "v001–v194 / w195–w214" are span descriptions, not density claims.
- `w195-qa` row: "2 broken" count is right (BUG-1 KB storm, BUG-3 scanned misclassification are the ❌
  rows); the README's parenthetical names the tone reset (BUG-2), which `w195-qa.md` files under a ✅ row.
- `.github/workflows/desktop.yml` triggers on `main` **and** `demo`; root README says "every push and pull
  request to `main`" — true but incomplete; left.
- Unit-suite claim "21 tests, ~0.1 s": 21/21 pass; wall time 0.23 s on this machine (test bodies ~3 ms).

## Gates

- `frontend/npm run build` exit 0 (pre-existing >500 kB chunk advisory only); `npm run lint` exit 0
  (the same 13 pre-existing advisories every recent lane records; docs-only change, no source touched).
- `data/kb` byte-identical to baseline (`git status` clean apart from the three doc edits + this file);
  the only processes started (fixture backend, vite dev) were this lane's own and were stopped after the run.
- Owner-identifier gate: `git diff --name-only dfce54f` contents contain no owner-identifying strings.
