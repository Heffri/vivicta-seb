# v006 — wallpaper, primitive states, table container hook: evidence

Lane `v006` (session `vivicta-glm-3`), branch `ao/vivicta-glm-3/v006-material` off `origin/acrylic` @ `c4dfcd6`.
Territory kept: `frontend/src/index.css`, `frontend/src/components/ui/{button,card,badge,select,table}.tsx`,
`docs/acrylic/DESIGN.md`, this directory. **`Card` needed no changes** — it is not interactive, so none of the
focus/hover/selected/disabled specs apply to it. Views, `App.tsx`, `shell/*`, `api.ts`, `types.ts`, `backend/**`
untouched.

## 1. Wallpaper rework (`--wallpaper`, both tones)

v001 copied UAW's *no-OS-material fallback* (a single dim radial over an almost-black linear). In a browser that
fallback is the permanent state, so it was carrying too little colour: the window read as flat grey. v006 keeps
the one-material rule (still only `.glass` on the shell blurs) and gives the ground a real composition:

| Token | Before (v001, UAW fallback verbatim) | After (v006) |
|---|---|---|
| `--wallpaper` dark | `radial(120% 90% at 50% -20%, #2a3350, transparent 60%)` over `linear(160deg, #11131b, #0a0a0d 60%)` | `radial(90% 75% at 14% 0%, rgb(56 96 178/26%), transparent 62%)` + `radial(75% 65% at 88% 100%, rgb(112 78 190/30%), transparent 60%)` over `linear(168deg, #1b2444 0%, #131830 45%, #0a0a12 100%)` — deep blue → indigo → near black, soft blue glow top-left, soft violet glow bottom-right |
| `--wallpaper` light | `radial(#cfd9f0 …)` over `linear(160deg, #eef0f6, #e2e5ee 60%)` | `radial(rgb(168 190 235/40%) …)` + `radial(rgb(196 178 228/28%) …)` over `linear(168deg, #f7f3e9 0%, #ececf6 45%, #e6e6f2 100%)` — creamy white → pale blue-violet, same two-glow construction |

Glow alphas were not freehand: they are capped so the *resolved* glass ground (88–90% `--bg-0` over the
wallpaper's brightest/darkest point) keeps v001's text-contrast ladder — see §2. Screenshots `extract-dark.png` /
`extract-light.png` show the result.

## 2. Contrast numbers (WCAG 2.x, computed by script, `saturate(180%)` shift < 1%)

**fg ladder on the worst resolved glass ground** (dark: `--bg-0` 88% over the blue-glow centre rgb(14 16 24);
light: `--bg-0` 90% over the lavender-glow centre rgb(249 248 252)):

| Step | Dark v006 | Dark v001 floor | Dark UAW flat ref | Light v006 | Light v001 floor | Light UAW flat ref |
|---|---|---|---|---|---|---|
| fg-0 | 17.31 | 17.32 | 17.90 | 17.10 | 17.08 | 17.61 |
| fg-1 | 9.45 | 9.46 | 9.77 | 10.03 | 10.01 | 10.33 |
| fg-2 | 7.33 | 7.34 | 7.58 | 7.79 | 7.78 | 8.02 |
| fg-3 | 5.57 | 5.57 | 5.76 | 5.88 | 5.88 | 6.06 |

No step regresses against v001 (max delta 0.02, light tone slightly better). The ~0.2–0.6 gap to UAW's flat
reading ground exists in v001 too — it is the cost of any wallpaper showing through 10–12% of glass, not of
this change. On card grounds (`--bg-3` over glass): dark 14.27 / 7.79 / 6.05 / 4.59, light 14.90 / 8.74 / 6.79 /
5.13 — same values v001's card surfaces had (the card token did not change).

**Status badge text (gate ≥ 4.5:1)** — text colour on its `-muted` tint over the two grounds:

| Tone | Tint | success | warning | danger |
|---|---|---|---|---|
| dark | 12% (unchanged) | 7.87 glass / 6.26 card | 7.69 / 6.12 | 6.19 / 4.95 |
| light | 12% (v001 value) | 4.88 glass / **4.31 card ✗** | 5.34 / 4.72 | 5.21 / 4.59 |
| light | **8% (v006)** | 5.16 / **4.55 ✓** | 5.67 / 4.99 | 5.54 / 4.87 |

So the light `--success-muted`/`--warning-muted`/`--danger-muted` tints go 12% → 8% (token change, dark keeps
12%; UAW's own light `.badge.ok` uses 10% — still short of the gate on the card ground). Foreground status
colours stay UAW-verbatim.

Primary button and ring: dark fg 9.23:1 / light fg 9.80:1 on their `--primary` washes; ring vs worst ground
7.55:1 dark / 6.04:1 light (non-text ≥ 3 gate).

## 3. Interaction states (additive; no props/exports/variant names touched)

- **Focus ring** — Button, SelectTrigger and Badge(-as-link) replace `focus-visible:border-ring ring-3 ring-ring/50`
  with `focus-visible:outline-2 outline-solid outline-offset-2 outline-ring`: a 2px solid accent outline with a
  2px offset whose gap shows the glass (Windows-11 style; a box-shadow ring with offset needed a solid offset
  fill, wrong on translucent glass). Verified settled computed style after Tab-focus:
  `outline solid 2px rgb(122 162 247) offset 2px` (dark accent). Note for whoever debugs this next:
  `transition-colors` in Tailwind 4 includes `outline-color`, so computed reads race a ~150ms transition from the
  base layer's `outline-ring/50` — wait before asserting.
- **Hover/active ladder** — outline/ghost buttons keep `hover:bg-muted` (`--bg-2`) and gain `active:bg-card`
  (`--bg-3`), one step up; `default` (primary) hover now uses a new **`--primary-hover`** token (dark
  `color-mix(in srgb, #7aa2f7 29%, var(--bg-0))`, light `color-mix(in srgb, #2b5cb4 24%, transparent)`) instead
  of `bg-primary/80`, which diluted the wash instead of raising the accent share the way UAW's own
  `.btn.primary:hover` does (dark 21→29%, light 16→24%). destructive hover was already a 10%→20% step; link and
  secondary unchanged.
- **Disabled** — `disabled:opacity-50 disabled:pointer-events-none` already in place on Button/SelectTrigger and
  inside the 40–50% spec; verified live: KB header `Compare` at opacity 0.5 + `disabled` before selection, 1.0 +
  enabled after two checks.
- **Select** — trigger gains `data-popup-open:bg-muted` (open = one step up, matching the outline button's
  `aria-expanded:` pattern; verified trigger bg rgba(32,32,39,.62) = `--bg-2` while open); items gain
  `data-highlighted:bg-accent data-highlighted:text-accent-foreground` and a persistent
  `data-selected:bg-accent/50 data-selected:text-accent-foreground` next to the existing check indicator
  (verified: first item bg rgba(48,48,57,.58) = `--bg-3`, `data-selected` + `data-highlighted` present).
  Disabled trigger/items keep their existing 50% + no-pointer treatment.
- **Table rows** — `TableRow` gains `aria-selected:bg-muted` so the ARIA-selected state matches the existing
  `data-[state=selected]:bg-muted`; verified identical resolved backgrounds when set either way, on a plain row,
  in both tones (dark rgba(32,32,39,.62), light rgba(0,0,0,.04)). Row hover verified changing live (KbView
  overlays its own accent hover on top of the primitive's `hover:bg-muted/50`; views are not my territory).
- **Scrollbar** — no change needed: `::-webkit-scrollbar-thumb` / `scrollbar-color` already ride `--line-2`,
  which is tone-aware; verified visually in both tones (`kb-*.png`, the 102-row table scrolls).

## 4. Table container hook

`Table` now takes optional `containerClassName?: string`, merged into the `table-container` wrapper div via the
same `cn` as everything else; default output byte-identical (`"relative w-full overflow-x-auto"` when absent).
Rationale recorded in DESIGN.md: the hardcoded `overflow-x-auto` forces `overflow-y: auto`, making the wrapper
the nearest scroll container and silently defeating sticky headers in bounded boxes (v005's workaround was an
arbitrary `[data-slot]` selector from outside). No view was switched over — views are not my territory; the
hook exists so the next one can.

## 5. Gates

`cd frontend && npm ci && npm run build && npm run lint` (Node 24 bundled runtime):

- `npm run build` — **green** (tsc -b + vite, built in ~9.3s).
- `npm run lint` — **green**, exit 0; the same 5 pre-existing `react(only-export-components)` warnings as
  v001/v005 (`ui/badge.tsx`, `ui/button.tsx`, `ResultsView.tsx` ×3), none introduced.

## 6. Screenshots — `docs/acrylic/evidence/v006/` (8: 4 dark / 3 light / 1 dark close-up)

Taken with the shared `shot.mjs` (headless Edge) plus a lane-local Playwright script for what its DSL cannot
express. Backend fixture mode on port 8023, frontend on 5223 via the shared lane vite config (`API_TARGET`
proxied to 8023, verified by reading `/api/config` through the proxy). `atlas_copco_2025.pdf` copied from the
shared lane dir into `data/reports/`, sha256 matched `data/kb/atlas_copco_2025/meta.json`.

- `extract-dark.png` / `extract-light.png` — (a) Extract empty state, full window: wallpaper glows + one glass pane.
- `kb-dark.png` / `kb-light.png` — (b) Knowledge base with **4C Group + AAK checked** (accent edge + tint on the
  selected rows, `Compare 2 selected` enabled), table + badges + sticky header.
- `results-dark.png` / `results-light.png` — (c) Atlas Copco income statement opened via `clicknth=Open|13`,
  `Export JSON` visible, real page images; success/danger badges on the new tints.
- `focus-ring-dark.png` — close-up after 5× Tab (clipped to the fiscal-year SelectTrigger): 2px accent ring,
  2px gap.
- `select-open-dark.png` — extra, for problem item 2: open popup (thin glass), selected item tinted + check,
  trigger one step up.

Tooling notes for the next lane (all hit and worked around here):

- `shot.mjs`'s `--tonekey` defaults to `tone`, which this app never reads — pass `--tonekey acrylic-tone` or the
  tone silently stays dark (my first "light" shot was dark).
- The work order's `click=Select 4C Group` cannot work in `shot.mjs`: those checkboxes have role `checkbox`
  (not `button`) and only an aria-label (no text), and `shot.mjs` tries exactly those two locators. Used
  `getByLabel(...).check()` in the lane-local script instead.
- Base UI's `SelectTrigger` sets `aria-labelledby` (accessible name = the current value), so
  `getByRole('button', { name: 'Fiscal year' })` matches nothing even though the trigger has `aria-label` —
  locate by attribute selector.

Screenshots checked: no owner identifiers, no filesystem paths rendered by the app.

## 7. Contract gap (recurring, documented)

Opening the Atlas Copco extraction through the running backend again rewrote `data/kb/atlas_copco_2025/
meta.json` + `pages.jsonl` (third documented occurrence after v005 and the supervisor's). Restored before
committing. The backend's re-registration path writes into tracked territory on a pure read flow — needs a
backend-side fix, out of scope here.

## 8. Not done / suggestions

- Views untouched, so the old hard-coded emerald/amber/red inside view files is still there (v002–v004 lanes'
  territory), and no consumer passes `containerClassName` yet.
- **Suggestion for the shell (outside my territory, needs a separate assignment):** the rail buttons and tone
  toggle still show the browser's *default* focus ring (style `auto`, tinted accent by the base
  `outline-ring/50`) — the primitive focus-ring treatment does not reach `components/shell/*`. One class-string
  change on the rail button (the same `focus-visible:outline-2 outline-solid outline-offset-2 outline-ring`
  recipe) would unify keyboard navigation visuals; visible in Tab order right before the primitives take over.
- No new dependencies, no animation additions, no router/state library.

## v006b — the wallpaper was invisible: shell glass transparency

Supervisor verdict on v006 at acceptance: structure fine, main goal missed — `--bg-0` was still 88% (dark) /
90% (light) opaque, so only ~10% of the wallpaper reached the eye and both tones still read as a flat
fill. v006b, same branch, changes only opacity knobs:

- dark `--bg-0` 88% → **66%**; light `--bg-0` 90% → **76%** (both inside the prescribed bands 66–72 / 72–78)
- dark `--bg-3` 58% → **64%** (the sanctioned +5–8 washiness allowance so cards stay solid over the livelier
  glass); light `--bg-3` keeps 6% — raising it darkens the card and *lowers* dark-text contrast, the wrong
  direction for the gate
- light `--success/-warning/-danger-muted` tints 8% → **5%** (why: see the sweep below — the clearer glass
  darkened the light card ground and success-on-card became the binding constraint)
- one-material rule untouched: still exactly one `backdrop-filter` (the shell `.glass`)

### Numbers — dark (worst ground = glass over the blue glow rgb(56 96 178/26%); violet glow checked, min 5.41 at 66%)

| `--bg-0` α | fg-0 | fg-1 | fg-2 | fg-3 |
|---|---|---|---|---|
| 88% (v006) | 17.31 | 9.45 | 7.33 | 5.57 |
| 72% | 16.36 | 8.93 | 6.93 | 5.26 |
| 70% | 16.23 | 8.86 | 6.87 | 5.22 |
| 68% | 16.09 | 8.78 | 6.82 | 5.17 |
| **66% (chosen)** | **15.96** | **8.71** | **6.76** | **5.13** |

Every swept alpha clears 4.5 for the whole ladder, so dark takes the most transparent end of the band. At 66%
with `--bg-3` 64%: badges on card 5.84 / 5.72 / 4.63 (success/warning/danger, 12% tints) ✓; fg ladder on card
13.39 / 7.31 / 5.67 / 4.31 — the 4.31 is fg-3, which is *defined but mapped to no shadcn variable* (grep: only
the two definitions in index.css), i.e. a ladder reserve, not a rendered combination; the lowest rendered text
token on card is fg-2 at 5.67 ✓. Primary fg 8.29, ring 6.96.

### Numbers — light (worst ground = glass over the darkest wallpaper point; the supervisor-prescribed brightest glow rgb(168 190 235/40%) is *milder* for dark-on-light text, min 5.68 at 76%)

| `--bg-0` α | fg-0 | fg-1 | fg-2 | fg-3 |
|---|---|---|---|---|
| 90% (v006) | 17.10 | 10.03 | 7.79 | 5.88 |
| 78% | 16.50 | 9.68 | 7.52 | 5.68 |
| **76% (chosen)** | **16.41** | **9.62** | **7.47** | **5.64** |
| 74% | 16.31 | 9.56 | 7.43 | 5.61 |
| 72% | 16.21 | 9.50 | 7.38 | 5.58 |

The ladder clears 4.5 at every alpha — the gate that actually binds is **success text on the light card ground**
(bg-3 6% over glass, 8% tint, worst wallpaper point):

| glass α \ tint | 8% | 6% | 5% |
|---|---|---|---|
| 78% | 4.40 ✗ | 4.51 | 4.57 |
| 76% | 4.37 ✗ | 4.49 ✗ | **4.55** |
| 74% | 4.35 ✗ | 4.46 ✗ | 4.52 |
| 72% | 4.32 ✗ | 4.44 ✗ | 4.49 ✗ |

No badge tint clears the gate at glass 72%, so the tone settles at **76% + 5% tints** — the most transparent
config the 4.5 gate allows. Final light numbers at that config: badges on card 4.55 / 5.00 / 4.89 (on glass
5.17 / 5.69 / 5.57) ✓; fg ladder on card 14.37 / 8.43 / 6.55 / 4.94 ✓ (fg-3 again an unmapped reserve);
primary fg 9.43, ring 5.80.

### Gates & shots

- `npm run build` **green** (494ms), `npm run lint` **green** exit 0 (same 5 pre-existing warnings).
- `v006b-extract-dark.png` / `v006b-extract-light.png` — Extract full window, both tones; the two glows are
  unmistakable under the glass (blue upper-left, violet/lavender lower-right) — this was the acceptance bar.
- `v006b-results-dark.png` — Results (Atlas Copco, opened via `Open|13`) over the visible wallpaper; status
  badges re-checked on the real data view.
- Backend/ports as in §6; `data/kb` re-rewritten by the read path on every Atlas open and `git restore`d before
  the commit — same contract gap, third + fourth occurrences now.

### Not done in v006b

- `--bg-1`/`--bg-2`/`--bg-4` untouched (they sit on the glass; their translucency now shows more wallpaper,
  which is the intended look, and they only ever darken the ground — contrast-safe direction).
- The select-open/focus-ring/kb screenshots from v006 above show the old 88/90% glass; they remain the record
  of the state assertions (class-level behaviour is unchanged by v006b), while §v006b shots show the new glass.
