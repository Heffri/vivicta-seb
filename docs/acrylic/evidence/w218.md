# w218 — repo final state: can a stranger clone and run it, and did we leave anything behind

Lane w218, 2026-09-22. Baseline `dfce54f` (the `release: keep main as Latest…` commit, tip of
`acrylic` and of `team/main` — audited state == what a stranger clones). Method: a fresh clone of
`https://github.com/Heffri/vivicta-seb.git` into a scratch dir, first-run driven strictly by
`README.md` / `CONTRIBUTING.md` / `run.bat`; then whole-tree scans, size accounting, and a
branch inventory. Zero model calls; `data/fetch.py` not run. Owner paths masked below as
`C:\Users\<owner>\…`.

## 1. The stranger path

Environment: Windows 11, Python 3.12.10 on PATH, Node **not** on PATH initially (Node v24.20.0
available from a local runtime dir, prepended to PATH for the second attempt — equivalent to a
stranger who has installed Node).

| # | Step | Result | Newcomer can self-serve? |
|---|------|--------|--------------------------|
| 1 | `git clone https://github.com/Heffri/vivicta-seb.git` | OK, 101.9 MiB, default branch `main` @ `dfce54f` | yes |
| 2 | `run.bat` (Node missing) | Stops before any long step with a one-line reason and the fix (excerpt below) | yes — message names the exact installer |
| 3 | `run.bat` (Node present) | Full first run in **~3 min 20 s** (README promises 2–4 min): venv → `pip install -r requirements.txt` → `npm ci` → `npm run build` → backend on `127.0.0.1:8000` serving the built frontend | yes — every step is logged with a `[run]` prefix |
| 4 | README "Try it in one minute" walkthrough | Verified over HTTP: `GET /` serves the built UI; `GET /api/config` → `{"llm":false,"provider":"fixture",…}`; `GET /api/library` → `[]` (no PDFs, as documented); `GET /api/kb?collection_name=all` lists `karnell_2025` (116 pages, `debt_maturity`); `GET /api/kb/karnell_2025/debt_maturity` returns the saved figures **with page + verbatim quote** (e.g. value 397.2, page 106, "Liabilities to credit institutions 43.5 353.7 - 397.2"); `GET /api/kb/karnell_2025/pages/116` returns page text **without the PDF on disk** | yes |
| 5 | `data/fetch.py` | **Not run** per lane rules. README states the cost (~130 MB) and, correctly, that nothing in the saved-library walkthrough needs it | yes |
| 6 | OCR prerequisites | `data/tessdata/` ships `eng.traineddata` + `swe.traineddata` + LICENSE in-repo (`scripts/setup_ocr.py` exists for extras) — nothing to download for OCR | yes |
| 7 | After the full run | `git status --porcelain` in the clone is **empty**: `.venv/`, `node_modules/`, `dist/`, the run log (`*.log`) are all ignored | yes |

Step 2 excerpt (verbatim):

```
[run] ERROR: Node.js was not found on PATH. Install Node 18 or newer from https://nodejs.org/ and re-run this script.

run.bat exited with code 1. See the message above for details.
```

Step 3 log (steps in order, paths masked):

```
[run] python 3.12.10, node v24.20.0, npm at <…>\runtime\node\bin\npm.CMD
[run] no .env in the repo root -> fixture mode (canned demo data, no model calls).
[run] backend/.venv not found, creating it ...
[run] installing backend dependencies ...
[run] frontend/node_modules not found, running npm ci ...
[run] frontend/dist missing or older than the source, building ...
[run] starting the backend on 127.0.0.1:8000 (serving the built frontend from the same port) ...
[run] ready: http://127.0.0.1:8000/
```

Port notes: the clone's server took :8000 (free here); if busy, `scripts/run.py` scans the next
19 ports and logs the switch. Stopping: this lane has no interactive console, so the server was
stopped by killing its process (the documented way is Ctrl+C, which `run.py` handles by killing
the backend it started).

**One blocker found, fixed in this lane** (one line): README entry point 2 described what
`run.bat` does but never said Node is required — a reader following the README alone hits step 2
surprised. Fixed by adding the Python 3.11+ / Node 18+ prerequisite to that bullet (see §5).

Judgement: no architectural repair needed. Every stopping point on the path either cannot happen
(with prerequisites present) or prints a self-explanatory message naming the missing piece.

## 2. Things that should not be left behind — whole-tree scan

All scans over tracked files at the baseline (not just the diff). No username-shaped string
occurs anywhere; the two `C:\Users\` grep hits are already-masked evidence lines.

| Pattern | Hits | Verdict |
|---|---|---|
| The owner's given-name fragment, and its Windows-login upper-case form (whole-tree, case-insensitive grep) | 0 | clean |
| `C:\Users\` | 2 files (`docs/acrylic/evidence/v158/replay-locate.md`, `replay-window.md`) | already scrubbed — both read `C:\Users\<owner>\.ao\data\...` with a placeholder; no identity left. Leave |
| `.ao\data` / `.ao/data` | same 2 masked lines | leave |
| `uaw-lanes` | 105 lines in 61 files, all under `docs/acrylic/evidence/` | all are relative mentions of scratch script names (`uaw-lanes/v004-compare-ask/shot-main.mjs`), never a username; the evidence archive is intentionally published. Leave |
| Machine ports | code/docs carry only the standard `8000`/`5173` and script `--api` defaults; lane-specific ports (8007, 8014, 5220, 8025, …) appear only inside evidence prose | historical test records, not identifying. Leave |
| PIDs | one PID (25816) twice, in v077/v088 evidence, describing a long-gone local process | leave |
| `AppData` | `%APPDATA%`, `Path.home()/"AppData"` generic forms only (locator code + docs) | clean |
| Tracked generated artifacts (`node_modules`, `dist`, `test-results`, `userdata`, `*.asar`, `*.pdf` in `data/reports/`) | 0 tracked | clean |
| Tracked-but-ignored (`git ls-files -c -i --exclude-standard`) | 0 — the recent `.gitignore` change (`data/kb/*/index.json` derived index) blocks nothing tracked | clean |
| Ignored-but-should-be-committed | `data/tessdata` whitelist (`!*.traineddata`, `!LICENSE`) keeps the two OCR packs committed; `frontend/.gitignore` covers `e2e/test-results`; `data/reports/` keeps `index.json` + `.gitkeep` | clean |
| Empty dirs in fresh clone | 0 | clean |
| Ad-hoc debug scripts | `scripts/` (21 files) and `eval/` are the documented checks/audits set; nothing stray at the root | clean |

`.gitignore` history check: the latest change (commit `01e1a0b`, the docs-readability pass) only
added `data/kb/*/index.json` — a derived artifact. Nothing it now ignores is tracked; nothing
that should be committed is ignored.

## 3. Repo size and largest files

Fresh clone (`git count-objects -vH`): **101.90 MiB** pack, 12282 objects, 1 pack — for a repo
that ships a 206-report saved library plus two OCR language packs. The ~130 MB of PDFs and the
~165 MB installer are downloads, not repo contents.

Top 10 blobs across all history — every one is intentional content, no accidental large file:

| Size | File |
|---|---|
| 3.97 MB | `data/tessdata/swe.traineddata` |
| 3.92 MB | `data/tessdata/eng.traineddata` |
| 1.81 MB | `data/kb/nordea_bank_abp_2025/pages.jsonl` |
| 1.64 MB | `experiments/laya/predictions.json` |
| 1.63 MB | `experiments/laya/dataset.json` |
| 1.40 MB | `data/kb/seb_2025/pages.jsonl` |
| 1.38 MB | `data/kb/swedbank_2025/pages.jsonl` |
| 1.27 MB | `data/kb/sv_handelsbanken_2025/pages.jsonl` |
| 1.16 MB | `data/kb/traton_2025/pages.jsonl` |
| 1.14 MB | `data/kb/ericsson_2025/pages.jsonl` |

(The 1.39 MB `data/kb/seb_2025/pages.jsonl` variant at #7 is the same file after a later rewrite
— history keeps both revisions; normal.)

## 4. Branch inventory (list only — nothing deleted)

Commits not in `acrylic` (`dfce54f`) are marked **unmerged**. "Real code" = touches files outside
`docs/`.

| Branch | Tip | Last commit | Merged? | Content | Suggestion |
|---|---|---|---|---|---|
| origin `acrylic` | `dfce54f` | 2026-09-22 19:13 | — | integration branch; == `team/main` | keep |
| origin `main` | `574fc64` | 2026-09-15 10:51 | merged | the pre-acrylic team `main` mirror (`docs: handoff for Boyu & Chen…`) | keep (mirror of upstream history) |
| origin `ao/vivicta-seb-132/w213-older-years` | `af3d53f` | 2026-09-22 15:40 | **unmerged** | real code: `backend: fix _year_ok accepting an incidental year mention anywhere in the body` + tests + evidence; self-merged acrylic-as-of-15:40, so it lacks the 19:13 tip | **merge first** — a real user-facing fix (older-years discovery); bring it up to `dfce54f` when landing |
| origin `ao/vivicta-seb-88/v166-balance-sheet-tie` | `08c9835` | 2026-09-21 16:10 | **unmerged** | real code, but a single "WIP: partial work saved by the supervisor" commit; touches `backend/schemas/debt_maturity.json` (schema edits invalidate every cached extraction) | delete, unless the owner wants to revive balance-sheet tie-out |
| origin `ao/vivicta-glm-53/v111-wip` | `d7505fa` | 2026-09-19 12:03 | **unmerged** | real code, single "WIP (lane stalled)" commit over `extract.py` / `test_confidence.py` / `debt_maturity.json` | delete — stalled WIP from three days before close, same schema caveat |
| team `main` | `dfce54f` | 2026-09-22 19:13 | — | the published final state | keep |
| team `demo` | `d652d10` | 2026-09-22 | merged (ancestor of `dfce54f`) | frozen demo-day build; target of the `desktop-demo` GitHub prerelease | keep |
| team `codex/historical-report-discovery-fixes` | `4c94188` | 2026-09-22 | merged (ancestor of `dfce54f`) | the historical-report discovery fixes, fully landed | keep (harmless; could be pruned by the team later) |

Per the lane rules no remote branch was touched.

## 5. Changes made in this lane

1. `README.md` — one sentence: entry point 2 (`run.bat`/`run.sh`) now states its prerequisites,
   "needs Python 3.11+ and Node 18+ on PATH" — the only newcomer blocker the fresh-clone pass
   produced, and a one-line documentation fix rather than a code change.
2. `docs/acrylic/evidence/w218.md` — this file.

Nothing else: no backend, frontend, schema, or script changes; the scan found nothing needing a
deletion. After the changes, the delivery check — `git diff --name-only <baseline>` piped through a
case-insensitive grep for the owner's name fragment — matches nothing.
