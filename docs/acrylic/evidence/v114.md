# v114 — debt_maturity hardening round 12: the seed-12 draw re-run under the full post-v111 stack

Lane `ao/vivicta-glm-56/v114-hardening-seed12`, from `origin/acrylic` at dispatch
`be1449f`. Measure-first, like every round since v063: quantify the complete current
mechanism stack — v095 (off-grid columns), v096 (finer-split sibling rows), v101
(sign normalization), v102 (date-form year headers), v103 (wrong-table guard, batch 1),
v104 (Swedish finer splits), v110 (subtotal-pair), v111 (wrong-table guard, batch 2)
and the v091/v109 top-level metadata keys — with `LLM_PROVIDER=codex`,
`LLM_MODEL=gpt-5.6-terra`, `EXTRACT_TWO_PASS=1`, `EXTRACT_QUOTE_RETRY` unset,
`LLM_BASE_URL` unset, `DEBT_BASIS` unset (**carrying** default; `GET /api/config` →
`"llm":true,"provider":"codex","maturity_basis":"carrying"`). Backend on PyMuPDF
**1.27.2.3**, `PARSER_VERSION = 6`. All 40 responses carry the 09-18 merge's review
fields (`issues`, `ready`, `check_history`, `basis`) — untouched here.

**Extract-layer headline: the two v111 guards and the v109 year key fired live for the
first time** — Volati's contract-liabilities timing fill and Momentum's Parent-Company
lease fills were refused in the live path (naming the table), and BTS's fresh
extraction now carries `buckets_by_year` straight off its p.92 year-rows table.

## The premise that broke: the Mid Cap universe is exhausted

The work order asked for **20 fresh seed-12 companies**. There are none left to
draw. Reproduced offline with random_check.py's own pool logic and the backend's own
`slugify` (zero fetches):

- Mid Cap in `data/companies.json`: **132**; minus Real Estate: **107** (the HANDOFF universe).
- Of those 107, **84** are excluded by random_check's eval-label-set filter — and every
  one of those 84 has already been drawn and sits in `data/kb` (label-excluded-but-
  never-drawn = **0**), so relaxing the label filter would surface nothing.
- The remaining pool is **23**: 20 already drawn (in `data/kb`) plus exactly the three
  stems earlier rounds recorded as permanent fetch failures — Cynca Nordic, Silex
  Microsystems, Stockholm Nordtech Group — all three re-verified live this round
  (full feed→search walk each, **still 404**; zero extract calls spent).
- Counting everything: `data/kb` carries **105** stems with a stored `debt_maturity.json`
  (the work order's parenthetical said 106) — 104 Mid Cap non-RE names + ericsson —
  so Mid Cap minus Real Estate is covered to the last name: **104 drawn + 3 never
  fetchable = 107**.

Reported to the supervisor immediately (reporting.md case 2, premise overturned), with
the default that was then followed: **the seed-12 shuffle's own 20 draws, re-run two
passes each under the current stack** — the same reinterpretation v090 used for its
already-seeded 15 (re-run to see the current state). The order is random_check.py's
own (`random.Random(12).shuffle` over the 23-company pool, skipping kb overlaps …
which here *are* the sample), so the sample is deterministic.

The 20, in draw order, with the stored answer that is their "before": BICO Group (s2),
Öresund (s7/fin), Arctic Paper (s11), Modern Times Group (s8), Cavotec Group (s11),
Viva Wine Group (s5), Volati (s11), BTS Group (s11), Enity Holding (s8/fin), Momentum
Group (s11), Green Landscaping Group (s7), Dustin Group (s11), Tången Industrikapital
(s8/fin), Acast (s2/s4), Mildef Group (s11), Byggmästare A J Ahlström H (s10), Hansa
Biopharma (s3), Orrön Energy (s11), Elanders (s11), Nobia (s11) — **10 of the 20 are
seed-11 companies extracted ~1 day earlier**, which makes them a direct same-company
A/B for everything that landed after v108's dispatch: v110, v111 and v109's live key.
Each was fetched once (fetch = 0 downloads: the 20 PDFs were pre-seeded from the
shared cache, **sha256 re-verified against each committed `data/kb/<stem>/meta.json`
before copying** — all 20 match, the v090 method) and extracted **twice** (`pass1`/
`pass2`, same page selection re-asked, the round's stability rule). Full responses in
[`v114/`](v114/).

## Driver rule (why no call was duplicated, LESSONS 37)

Same rule as v082/v088/v090/v097/v108, built into a small driver
(`uaw-lanes\vivicta-glm-56\driver.py`, not part of the repo): before calling
`/extract` for a `(stem, pass)` pair it checks whether the evidence JSON already
exists and only calls the backend when it does not; every response is written to its
exact evidence path immediately on return. `urllib` timeout 600 s per call; on a
client timeout it would wait 660 s and **not** resend (no timeout occurred). Two
disclosures: (1) the driver's first launch walked only the three 404 stems and a
second launch crashed **between** companies on a console-codepage print (GBK console
vs a Swedish company name) after saving BICO's two passes — zero calls attributable
to either; the relaunch under `PYTHONUTF8=1` re-checked the completed files and
logged "evidence already on disk, not re-sent", so no pass was ever sent twice.
(2) Route times 21–103 s (mean 38 s).

## Results — 20 companies, two passes each

Strict scale as every round: full = all four fields explicit at 1.0 and every check
passed. **满 0 / 部分 17 / 失败 2 / 无效 1** (the invalid entry is a corpus defect,
below). Value-for-value pass1==pass2 stability: **16/19 valid companies** (unstable:
Arctic Paper, Momentum, Volati; Modern Times' nulls are not a measurement).

| Company | Two-pass pick (p1 → p2) | Fields (best pass) | Verdict |
|---|---|---|---|
| BICO Group | `[86,87]` both | all null | **partial, honest** — its only maturity table (p.86) is titled "Maturity structure for financial liabilities, **including future interest payments (non-discounted amounts)**" with trade payables rows inside: out of scope by the v028 convention, correctly declined twice. Same as stored. |
| Öresund | none — **candidates `[]`** | all null | **failed** — unchanged since v090: nothing in the report matches any schema keyword (hybrid bonds + leases under other names); no page-select call. |
| Arctic Paper | `[66,67]` both | p1 w1y **219,487@0.9** (p.66 short-term table's `Total`); p2 total **251,209@0.85** (p.75 `Interest-bearing bank loans (incl. cash pooling liabilities)`) | **partial, unstable** — the v108 instability repeated in a new form: each pass read one half, and p2's 251,209 is the combined total v108's "for the group" wanted, from the balance-sheet row, but never alongside the 219,487/31,722 pair. Stored answer (219,487 + 31,722) not reproduced. |
| Modern Times Group | `[99,100]` both | all null | **invalid — wrong document** (corpus defect, see below): `modern_times_2025` is MTG's FY**2021** annual report; every MTG reading ever recorded, stored or fresh, read it. |
| Cavotec Group | `[71,72]` both | total **23,702@0.9** | **partial, byte-stable, honest** — same as stored. Older layers observed working: the row-level year guard refused a 2024-column lease row (`'Lease liabilities 3,805 8,756 4,621 2,689' names fiscal year 2024`), the bare-Total demotion refused `Total 39,403 …`. |
| Viva Wine Group | `[113,114]` both | all null | **partial, honest** — p.113's only maturity analysis is the GROUP `MATURITY ANALYSIS` over all liabilities (`Total 611 298 393 1,021 2,323` with trade payables 862 and derivatives inside; undiscounted wording on the page): declined by convention. Same as stored. |
| Volati | `[177,178]` both | total **3,246@0.9**; w1y **192@0.25** (p1) / null (p2) | **partial, unstable — but the v111 guard's round**: the model's own 192 (Note 17's printed current total, its quote failing verification → 0.25) **survived in p1**, and in both passes the fill that used to overwrite it with 87 was refused — `fill from page 177 row 'Within 1 year 87 87 1' refused -- table 'Timing of revenue recognition, contract liabilities' is a non-debt subject table` (p1: fill-time; p2: the model-null variant of the same gate). The stored 87@1.0 mis-scope is dead. |
| BTS Group | p1 `[91,92]`, p2 `[92,93]` | total **579,797@0.9**; w1y **77,141@0.25**; 1–5y **502,656@0.25** (p1) / **@0.9** (p2, `Non-current liabilities` row); >5y null | **partial, value-stable — `buckets_by_year` live in both passes**: years 2026:77,141 / 2027:39 / 2028:300,039 / 2029:202,539 / 2030:39 off p.92's own year-rows table, sum = total, the v109 continuation's shape — the first fresh extraction carrying the key (the stored answer only got it via the v109 republish). The year-label quote-join still caps w1y at 0.25 (v108 gap 2); p2's 1–5y reached 0.9 via the note's non-current row. No `later` row → >5y honestly missing. |
| Enity Holding | `[81,82]` both | all null | **partial, honest** — the bank shape (v090): p.81's table is the undiscounted contractual analysis, two-sided, deposits inside. Same as stored. |
| Momentum Group | p1 `[116,117]`, p2 `[111,112]` | p1 total **622@0.9** (`Interest-bearing liabilities`), buckets null; p2 all null (selection moved off the total's page) | **partial, unstable — v111's second live guard**: all three bucket fills from p.111's Parent-Company lease table refused in **both** passes — `fill from page 111 row 'Within 1 year 2 2' refused -- the Parent Company section 'Parent Company' holds this table` (×3 rows ×2 passes). The stored 2/2/0@0.5 mis-scope is dead; 622 carries 0.9 (stored 0.9). |
| Green Landscaping Group | `[99,100]` both | all null | **failed** — p.99's credit-risk note prints a maturity table whose **debt rows reconcile per row** (`Liabilities to credit institutions 1,995 19 68 1,898 10 1,995`; `Bonds 500 500 500`; `Lease liability 712 41 171 451 49 712` — 41+171+451+49 = 712) beside payables/earn-out rows, with **no undiscounted title marker** — v103's refinement deliberately does not refuse it and v095's debt-row-first ordering can read it, but the model declined it twice and no repair path exists for a model that answers null without a verifiable quote. The round's one live Karnell-family decline. |
| Dustin Group | `[116,117]` both | total **3,055@0.9** (the roll-forward `Total`, p.117) | **partial, byte-stable, honest** — same as stored: p.116's `Maturity structure` is contractual (Group and Parent side by side), declined by basis. |
| Tången Industrikapital | `[63,64]` both | total **915,454@0.95** (`Total upplåning (inkl leasingskulder)`, p.57); buckets null | **partial, value-stable** — page selection never surfaced the p.62 maturity note (candidates `[57,58,63,60]`), so the buckets the stored v090-era answer once had stayed unread; the total is the report's own printed incl.-leases row (the p.62 note's two rows sum 588,199 + 154,854 = 743,053 — a different scope than the key-ratios row it prints; the report's own two numbers disagree). Conf 1.0→0.95 vs stored total, value identical. |
| Acast | `[65,66]` both | all null | **partial, honest decline** — the lease note's year buckets (32,511+32,692+32,404+32,404+45,414 = 175,425) do not close on the carrying 141,152, and the over valve refused both column reads with the rows named; the model's own answer was null. Stored total 135,382@0.9 (seed-4 era) not reproduced — a variance loss recorded as-is. |
| Mildef Group | p1 `[95,96]`, p2 `[112,113]` | total **772.9@0.95 / @1.0** (p.112 `Total borrowing`, both passes) | **partial** — current/non-current granularity only (v048 shape); selection moved, the value's page did not. Same as stored. |
| Byggmästare A J Ahlström H | `[54,55]` both | total **2,873@0.9**; w1y **962@0.9**; 1–5y null (`1911 is printed on none of pages [53,54]; dropped as computed, not read`) | **partial, byte-stable — improved**: w1y 922@0.95 (stored) → **962@0.9**, the correct `Amotering år 1` 2025 figure (v104's page walk); the 1,911 (=år 2+3+4) computed-drop and the cross-page per-year rows are v104's recorded open gap, unchanged. |
| Hansa Biopharma | `[65,66]` both | total **927,403@0.9** (`Long-term and short-term loan`, p.66; the currency warning names the SEK-thousands resolution) | **partial, byte-stable** — total only, no maturity table; same as stored. |
| Orrön Energy | `[67,68]` both | total **106.4@0.9**; w1y **0@0.9** (the dash current row, quote-context provenance) | **partial, byte-stable, honest** — current/non-current only (v048). Same as stored. |
| Elanders | `[141,142]` both | all null | **partial, honest** — v108's reading unchanged: carrying note prints instrument rows only; the sole maturity table (p.142 `Due date structure`) is undiscounted. |
| Nobia | `[131,132]` both | total **2,983@0.9**; w1y **0@0.5**; 1–5y **2,983@1.0**; >5y **0@0.5**; check **passed** | **partial, byte-stable** — the v097 Rejlers family again: the table's own dash zeros answered 0 and capped at 0.50 by the zero-quote gate; full confidence structurally unreachable. Same as stored. |

**Full confidence: 0; partial: 17; failed: 2** (Green Landscaping, Öresund); **invalid:
1** (Modern Times — wrong document, excluded from the denominators). Value-for-value
stability: 16/19 valid. Page-selection stability: 17/19 (Mildef's and Momentum's p2
picks moved; Mildef kept the value page, Momentum lost the total).

## The corpus defect: `modern_times_2025` is the FY2021 report

`data/kb/modern_times_2025/meta.json` records
`source_url: …/2022/04/MTG-Annual-and-CR-Report-2021-eng.pdf`, pages: 148, and the
parsed text is MTG's "Annual and Corporate Responsibility Report **2021**" cover to
cover (122 of 148 pages contain "2021"; six contain "2025" — forward-looking targets).
fetch.py's year validation (`_year_re`: the report must mention the year **somewhere**)
is satisfied by those six mentions, so a wrong-year document passes and was stored as
`modern_times_2025` — every MTG extraction since (seed 8, the republishes, this
round's two passes) has read the wrong report. The same sha-verified PDF is in the
shared cache, so this is not a copy error of this round. All other 19 documents
verified: each cover names 2025 (Dustin's FY spans Sep 2024–Aug 2025 and is the
correct FY2025 report). Fix is fetch.py territory (validate the fiscal year on the
cover/first pages, not anywhere) plus a re-fetch of the stem; not this lane's
territory, reported to the supervisor.

## Tonight's mechanism hit table

Same convention as v082/v097/v108: a dash means no attributable marker in the saved
responses, not proof of absence. Nine columns per the work order: v095, v096, v101,
v102, v103, v104, v110, v111, and the v091/v109 metadata keys.

| Company | two-pass | v095 off-grid | v096 finer-split | v101 sign | v102 date-years | v103 guard-1 | v104 Swedish | v110 subtotal-pair | v111 guard-2 | v091/v109 metadata |
|---|---|---|---|---|---|---|---|---|---|---|
| BICO | ✓ | – | – | – | – | – | – | – | – | – |
| Öresund | – (no candidates) | – | – | – | – | – | – | – | – | – |
| Arctic Paper | ✓ | – | – | – | headers on 5 candidate pages; no correction needed or fired | – | – | – | – | – |
| Modern Times | ✓ (wrong doc) | – | – | – | – | – | – | – | – | – |
| Cavotec | ✓ | – | – | – | headers on p.71/72; none fired | – | – | – | – | – (older layers: row-year guard + bare-Total demotion, see results) |
| Viva Wine | ✓ | – | – | – | headers on p.113/114; none fired | – | – | – | – | – |
| Volati | ✓ | – | – | – | – | – | – | – | **✓ fill refused ×2 passes** (non-debt timing table) | – |
| BTS | ✓ | – | – | – | – | – | – | – | – | **✓ `buckets_by_year` both passes** (v109; `prior_year` none) |
| Enity | ✓ | – | – | – | – | – | – | – | – | – |
| Momentum | ✓ | – | – | – | – | – | – | – | **✓ fills refused ×3 rows ×2 passes** (Parent Company section) | – |
| Green Landscaping | ✓ | – | – | – | – | – (markerless table: deliberately not refused) | – | – | – | – |
| Dustin | ✓ | – | – | – | – | – | – | – | – | – |
| Tången | ✓ | – | – | – | – | – | – | – | – | – |
| Acast | ✓ | – | – | – | – | – | – | – | – (over-valve declines, not the guard) | – |
| Mildef | ✓ | – | – | – | – | – | – | – | – | – |
| Byggmästare | ✓ | – | – | – | headers on p.54/38; none fired | – | – | – | – | – |
| Hansa | ✓ | – | – | – | – | – | – | – | – | – |
| Orrön | ✓ | – | – | – | – | – | – | – | – | – |
| Elanders | ✓ | – | – | – | – | – | – | – | – | – |
| Nobia | ✓ | – | – | – | – | – | – | – | – | – |

Tally: two-pass 19/19 valid companies (Öresund has no candidates to select from; no
single-pass fallback anywhere). **v111: 2 companies live — both exactly the shapes it
was built for, refusing at the fill in every pass. v109: 1 company live (BTS), the
first fresh extraction carrying `buckets_by_year`. v091 `prior_year`: 0.** Every other
column 0 attributable events — no off-grid columns, no finer-split siblings, no
liabilities-negative signs, no date-form year headers over maturity-table rows (they
abound on balance-sheet-class pages — 12 of the 20 companies — and no wrong-year
correction fired anywhere, v102 doing its quiet job), no refused undiscounted/all-
liabilities citations (BICO/Viva Wine/Elanders/Enity declined on their own; the guard
never had to engage), no Swedish finer splits, no two-section subtotal pairs. Older
layers observed: Cavotec's row-level year guard and bare-Total demotion; the
computed-not-read guard on Byggmästare's 1,911 and BTS's year-label sums.

## Before/after against the stored answers (same company, earlier round)

- **Value-identical: 12** (BICO, Cavotec, Dustin, Elanders, Enity, Green Landscaping,
  Hansa, Mildef, Nobia, Öresund, Orrön, Viva Wine).
- **Confidence-only: 1** (Tången 915,454 @1.0 → @0.95).
- **Improved: 3** — Volati (w1y null → **192**, the model's own answer, now that the
  fill that used to overwrite it is refused), Byggmästare (w1y 922 → **962**, the
  correct år-1 figure), BTS (1–5y @0.25 → @0.9 in pass 2, plus the live `buckets_by_year`).
- **New but unstable: 1** (Arctic Paper pass 2's 251,209 — a total the stored answer
  never had; pass 1 lost the stored 219,487/31,722 pair).
- **Worse: 1** (Acast: stored total 135,382@0.9 → null twice — the stored figure was a
  seed-4-era read whose own row arithmetic never closed; the new declines are honest
  but the round records the loss as a variance loss, not a fix).
- **Invalid: 1** (Modern Times — wrong document; its stored nulls were never a
  measurement of MTG's FY2025).

## Schema decision: no candidate, no commit

`backend/schemas/debt_maturity.json` is byte-identical to the dispatch baseline
(`git diff be1449f -- backend/schemas` empty), and the round's backend diff is empty
by construction. No four-key vocabulary gap surfaced: Green Landscaping's debt rows
are already `row_synonyms`-covered ("liabilities to credit institutions", "lease
liabilities" — the decline is model behaviour, not vocabulary); no new bucket
wording, off-grid label, Swedish finer split or date-form header appeared over any
maturity table in the 20; and the two mis-scoped fills this round killed were killed
by the already-landed v111 guards, not by words this round could have added. Zero
schema commits, as v088/v108 before this.

## Gap count (primary cause per counted company)

1. **Model decline on a readable, per-row-reconciling maturity table: 1** (Green
   Landscaping) — the markerless all-liabilities-shaped credit-risk table: the debt
   rows close per row, v103 deliberately does not refuse it, v095's ordering can read
   it — but every fill path needs a model total or a verified quote, and the model
   answered null twice. Extract layer: the missing piece is a row-level fill that
   does not depend on the model's own answer (v095's debt-row-first ordering runs
   inside `_fill_bucket_columns`, which needs a header the torn page never yields).
2. **Locator blindness: 1** (Öresund, zero candidates since v090 — unchanged).
3. **Undiscounted / all-liabilities maturity analyses declined by convention — honest
   nulls: 6** (BICO "non-discounted amounts" title; Viva Wine; Elanders; Enity (bank);
   Dustin (contractual, Group+Parent in one row); Cavotec (NOTE 5 contractual)).
4. **Current/non-current granularity only (v048 shape): 2** (Mildef, Orrön).
5. **Printed-dash zeros answered 0, capped at 0.50 by the zero-quote gate (Rejlers
   family): 1** (Nobia; check passes, full confidence unreachable).
6. **Per-year / cross-page shapes still open: 2** (BTS — the year-label quote-join
   caps w1y/1–5y at 0.25 whenever the note's own rows are the only source, v108 gap 2
   in its narrow form; Byggmästare — cross-page per-year rows, v104's recorded gap:
   1,911 still dropped as computed).
7. **Instability across passes: 3** (Arctic Paper — the short/long pair and the
   combined total never appear in one answer; Momentum p2 and Volati p2 — selection
   moved or the model's own quote failed; in all three the values the guards kept are
   the right ones).
8. **Lease-table geometry: 1** (Acast — the year buckets sum 175,425 against a
   carrying 141,152; the over valve's declines are correct and the company stays
   unread).
9. **Corpus defect (not an extractor gap): 1** (Modern Times — FY2021 PDF stored as
   FY2025; fetch.py's year validation is satisfiable by any mention of the year —
   fix + re-fetch named above).

## Regression and verification

Two attestations, one per tree state; **this round's backend diff is empty by
construction** (no schema commit, no code commit — the delivery carries docs only),
so these are baseline attestations in the v088/v108 sense, plus the merge gates the
work order ordered.

**This round's own runs** (dispatch tree `be1449f`, before any edit):

- `python -m pipeline.test_confidence` / `test_parse` / `test_kb` / `test_paths` /
  `test_llm` — all green on the pristine dispatch tree before any fetch or extract.
- `label_regression.py --schema income_statement --schema debt_maturity --baseline
  be1449f` on the pristine tree (both arms the same code): **identical=1756,
  changed=16, skipped=884** — the 16 are the documented verbatim-vs-cleaned
  baseline-arm asymmetry rows (duni, enea, medcap ×2, nobia ×2, rejlers, rusta and
  the income_statement counterparts), pre-existing, 0 attributable to anything.

**Delivery tree** (after merging `origin/acrylic` at `c10b310`: v116's 15-stem data
republish + docs, v112's frontend KB switch, v121's optional `LLM_STRICT_SCHEMA` in
`llm.py` — `git diff be1449f origin/acrylic -- backend/pipeline/extract.py
backend/schemas/debt_maturity.json backend/pipeline/parse.py` is **empty**, so every
mechanism this round measured is on the merged tree byte for byte; v121's llm.py flag
is off by default and not exercised by replay):

- The five test modules again all green on the merged tree.
- `replay_check.py --section debt_maturity` over this round's 20 stored answers (the
  post-run KB entries, replayed through the merged `extract()` vs the dispatch sha
  `be1449f`, per the work order's fetch-then-replay clause): **19 companies, 19 same,
  0 changed** — the merged code moves none of this round's answers (Öresund is
  not_replayable: zero candidates, pre-existing).
- `replay_check.py --section debt_maturity` over the full corpus (`data/kb` + seed
  1..11-kb + seed-fin-kb, baseline `be1449f`): **223 companies, 223 same, 0 changed,
  116 not_replayable** (code identical; v116's republished stems replay same through
  the same extract()).
- `label_regression.py` on the merged tree vs `be1449f`: **identical=1766,
  changed=16, skipped=874** — the same 16 changed rows as the pristine run (duni,
  enea, medcap ×2, nobia ×2, rejlers, rusta and the income_statement counterparts;
  the tool's documented verbatim-vs-cleaned baseline-arm asymmetry), with ten rows
  moved from changed/skipped into identical by v116's republished stored answers.
  **0 True→False and 0 attributable to this lane** (its backend diff is empty).

## Call count and weekly usage

Backend `[extract]` log lines: **40** (20 companies × pass1/pass2, one route each;
the round's call basis; cap 50). Control-flow accounting for transparency: each route
is page-select + main = 2 Codex calls, except Öresund's two routes (zero candidates —
no page-select, 1 call each); the silent widen fallback (a third call on `pages[:4]`
when the two-page window answers ≤1 non-null field, ≥3 candidates) accounts for at
least the 23 routes whose final answer held ≤1 non-null field — **≈101 actual model
invocations** (a lower bound: widens that themselves produced a second field are
indistinguishable post-hoc, e.g. Arctic Paper pass 2's total). No resend ever
happened. Fetch-side: 3 × 404 walks (the standing stems) + 20 sha-verified cache
copies, zero model-search fetch calls. `provider-usage.py`: codex seven-day
**79% → 83%** (resets 09-20 12:45; the 85% stop never reached).

## Housekeeping

`data/kb` working-tree changes from this round's runs (the 20 re-extracted
`extractions/debt_maturity.json`, BICO's one-time parser 3→6 meta/pages upgrade — the
LESSONS-27 legal re-registration, sha unchanged — and the `data/reports/index.json`
entries for the pre-seeded PDFs) are **not committed** per the round rule. They are
superseded on the merged branch for Volati/Momentum by v116's republish; the full set
of this round's stored-answer files is preserved for the supervisor's `seed12-kb`
collection in the lane directory (`uaw-lanes\vivicta-glm-56\datakb-after\`, outside
the repo) and, in extraction-response form, in [`v114/`](v114/). The `docs/` files
the evidence commits (this file + 40 response JSONs) were checked for codex error
text and machine paths — none present (the round's responses carry no llm warnings;
the one stored-answer timeout text quoted in the before/after work is already masked
in the committed stored file). Owner-identifier grep over every committed file —
empty.

## For the group (what a future lane should pick up)

1. **The universe management question**: random_check.py's label-set exclusion and
   the drawn set now cover all 107 Mid Cap non-RE names; every future "N fresh
   companies" work order needs either a new universe (Large Cap), a re-run protocol
   (this round's pattern worked and paid for itself — two live guard hits and one
   live key from re-runs), or an explicit policy change.
2. **Green Landscaping is the cleanest open extract-layer target**: a table whose
   debt rows reconcile per row, markerless, torn header — a row-level fill that can
   run without the model's own total (the v095 ordering without `_fill_bucket_columns`'
   header dependency) would read 1,995/500/712 and their buckets off the printed rows.
3. **fetch.py's year validation** should anchor on the document's own cover/fiscal
   period, not any mention of the year — `modern_times_2025` is the proof it can
   store a five-year-old report under this year's name (and MTG needs a re-fetch).
4. **Nobia's dash zeros** remain the standing printed-nil-cap case (v090 Linc, v097
   Rejlers, v108 Nobia, now again) — the stamp-`printed_nil`-on-a-model-answer
   extract ruling is still the unlock for the one family that keeps honest companies
   at 0.50 forever.
5. **Arctic Paper** still never reads its short-term + long-term totals in one answer
   (v108's item 4, third round running) — the note's own two printed totals joining
   into 251,209 remains the one-line schema-description ruling it was parked on.

## Delivered

Branch `ao/vivicta-glm-56/v114-hardening-seed12`, pushed to `origin` only. Commit:
`docs:` this evidence file + the 40 per-company response copies, on top of the
delivery merge of `origin/acrylic` `c10b310`.
