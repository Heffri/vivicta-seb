# v167 — DEMO.md, README first screen, honest accuracy framing

Lane `ao/vivicta-glm-98/v167-demo-and-readme`, off `origin/acrylic` `6f43cd2` (the PR #6 merge —
the delivery fetch re-checks `origin/acrylic` and merges again if it moved). Docs-only lane: no
`backend/`, `frontend/`, `schemas/`, `eval/` or `data/` byte touched, zero model calls.

Continuation: the earlier v167 session's half-done work (`ao/vivicta-glm-94/v167-demo-and-readme`,
one WIP commit `11d90b5` saved by the supervisor at the owner's pause) is merged into this branch
as-is — it contains exactly the three step screenshots below and nothing else; no text artifacts
existed on it. The screenshots were captured by that session (fixture backend, Karnell opened from
the KB, dark tone, viewport capture, never full-screen); this session verified their content
against the current tree before writing any text around them.

## What was delivered

| File | Change |
|---|---|
| `docs/DEMO.md` (new) | The frozen demo pack: version lock (acrylic sha + installer feed names), the two bundled samples (Karnell/XANO: pages, values, check states, PDF availability) plus Ericsson as the honest-incomplete case, the three-minute script (six segments adapted from consult gpt6 #3 into this repo's product terms; stored results are the main show, live extraction an encore), the demo-day hardening checklist (consult fable #11: clean-userData closed loop, offline-readable, no web-search/OCR dependence, one redistributable real PDF, pre-warming, Settings test, package sanity), and the "what we cannot promise" list (identity closed ≠ basis right; null ≠ 0; verified quote ≠ report correct; confidence ≠ accuracy probability; curated score ≠ first-pass ≠ market rate; fixture is fictional; scope limits) |
| `README.md` | First screen rewritten: one-line positioning ("an annual report's debt note in → analyst material with sources out"), the three-step screenshot strip (`evidence/v167/`), a "try it in one minute — no model, no download" path (KB → Collection: All → `karnell` → Open), three install/run entry points (auto-updating `desktop-demo`/`desktop-main` installers; `run.bat`/`run.sh`; from source) with the non-auto-updating portable `desktop-0.3.4` explicitly demoted, pointers to `docs/DEMO.md` / `docs/API.md` / `docs/acrylic/README.md`; new "Accuracy — the honest version" section (below); stale lines reconciled with the current version: the Deferred list no longer claims multi-report compare is deferred (Compare ships; selective page OCR ships; ESEF/iXBRL, async jobs, DB, auth remain), the fetch note now says 114 curated index entries (was "six"), the Desktop app section points at the auto-updating feed |
| `docs/acrylic/README.md` | "What a colleague sees now" Accuracy bullet replaced: stored-library vs first-extraction kept as two labelled claims plus error-nature and boundary bullets; 86.4% / 81.5% superseded by this tree's re-measurement |
| `docs/PERFORMANCE.md` | A header note marks every number on the page (26.748 s median included) as the 20 September revision's measurement, names what the 21 September integration changed underneath it (shared `pipeline.llm`, BM25+embeddings retrieval, parser 7 + selective OCR), and states nothing was re-measured |
| `docs/acrylic/evidence/v167/` | The three step screenshots (this file's predecessor session) |

## The numbers, re-measured on this tree

All commands offline; no API, no network, no model:

```
python eval/run.py --stored-kb data/kb --no-fail
  → value accuracy: 327/367 (89.1%)
  → page hit-rate: 263/313 (84.0%)
python scripts/eval_breakdown.py --stored-kb data/kb --section debt_maturity
  → debt_maturity 271 rows: values 231/271 (85.2%), pages 171/221 (77.4%)
  → miss buckets: A 29 rows/21 companies, B 4/3, C 14/12, D 7/6
python scripts/eval_breakdown.py --stored-kb data/kb   # income: 96/96 values, 92/92 pages
```

A direct pass over `eval/labels.csv` + the stored extractions reusing `run.py`'s own
`values_match` classifies the 40 debt value misses: **34 output-empty, 6 non-empty mismatched**
(better_collective, bergman_beving ×2, net_insight, proact_it, volati). The v154 audit supplies
the error *nature*: 0 label errors, 2 report-internal disagreements (green_landscaping, volati),
7 label-right hard cases with named mechanism gaps.

First-extraction batches (the labels never entered those runs; they scored afterwards): v124
9 → 14 of 24 companies; v136 stored 16/40 → 19/40 after the round's publish set (raw fresh union
14/40); v160 stored 23/69 → 31/69 fields, pages 17/62 → 23/62. These transitions are quoted in
both READMEs with the raw fresh scores alongside where they differ from the published set.

The two packaged samples were read from `data/kb/` directly: Karnell (`karnell_2025`, en, 116 p)
— 397.2 / 43.5 / 353.7 / 0 MSEK all sourced to page 106, check passed, `maturity_basis: carrying`,
no human review; XANO (`xano_industri_2025`, sv, 164 p) — 904,522 / 51,075 / 802,669 / 50,778
TSEK all sourced to page 84, check passed. Ericsson (`ericsson_2025`): 32,703 + 3,538 on p.65,
two nulls, check unclosed (`missing: due_1_to_5_years`) — the honest-incomplete case. PDF
availability: `data/reports/*.pdf` is gitignored and the desktop bundle ships none, so **no PDF
ships** with either repo or installer; both samples' `meta.json` record public `source_url`s, and
a no-PDF open still serves values, quotes, checks, review and export (only page images wait for
the PDF).

## Screenshots (captured by the predecessor WIP session, verified here)

1. `step1-kb-open-karnell.png` — Knowledge base, Collection: All, search `karnell`, 1/206, the
   Karnell Group row (FY2025, 116 pages, Debt maturity structure) with **Open**; status bar shows
   the fixture backend (`fixture · bge-m3`) — no model configured.
2. `step2-buckets.png` — Results for that record: the four buckets (397.2 / 43.5 / 353.7 / 0
   MSEK), "Needs review" chips, "Calculated from report" on the derived 0, the review form, and
   the Source panel naming page 106.
3. `step3-source-page-quote.png` — the 1–5-year row selected: page 106's image (NOTE 20
   BORROWINGS) and the verbatim quote `liabilities to credit institutions 43.5 353.7 - 397.2`
   with the value highlighted.

## Gates

- Diff is markdown-only (`README.md`, `docs/DEMO.md` new, `docs/PERFORMANCE.md`,
  `docs/acrylic/README.md`, this file + the three PNGs): no source, schema, test or data byte.
  `npm run build`/`lint` not re-run — this fresh worktree has no `node_modules` and no file the
  build reads changed; the frontend/backend source trees are byte-identical to `origin/acrylic`.
- `data/kb` untouched (the samples were read, not written); `git status` carries only the five
  doc paths.
- Every number asserted above was produced by a command in this worktree (the two READMEs and
  DEMO.md quote only these or evidence-cited earlier runs, labelled as such).
- Owner-identifier scan over the changed files: run separately before the push, per the usual
  gate discipline.
