# v058 — present-label guard scoped to the operands' own table, not the whole page

Branch `ao/vivicta-glm-17/v058-present-label-table-scope`, off `origin/acrylic` (`ee391d8` at dispatch),
merged forward to `origin/acrylic` (`b83c0c6`, which already contains `ao/vivicta-glm-16/v054-quote-retry` —
the work order's conditional merge — plus v049/v053/v055–v057/v059) before the final gates; `_check`'s
guard was untouched by all of them, so the mechanism carries over unchanged. Offline work: no services, no
model calls, `data/` untouched (`git status --short data/` empty).

## The gap

v044's present-label guard (a null `null_as_zero` bucket is only zero-filled when its own synonym label is
absent from the search rows) searched **every row of every candidate page**. Swedish notes routinely stack
several tables per page, so a bucket word printed by a *different* table blocked a 0 the operands' own table
really implies: MedCap's carrying-amount maturity table (p.101) has no `>5 år` row, but the contractual
cash-flow table's `>5år` header (p.102) sat in the candidate-page search, so `due_after_5_years` stayed out
of the zero-fill and `maturity_sums_to_total` sat at `missing: due_after_5_years` although
54.3+48.0+616.5 = 718.8 ≈ 718.7 closes the identity.

## What changed (2 files, +118/−29 vs the merged base)

- **`backend/pipeline/extract.py`** — `_check`'s search rows are now, when provable, the rows of the
  operands' own table; new helper `_operand_table_rows` (placed next to `_row_year_column`, whose upward
  walk it reuses via `_year_run`). **Table-span definition:** anchor each *real* operand of the identity's
  expr — `total_debt` included, so the span reaches the Totalt row — to its exact `_page_rows` row (the same
  standard `_column_values`/`_derived_value` anchor rows by); per page, the span runs from the nearest
  year-header row above the page's topmost anchor (`_year_run` over the growing window; the page top when no
  header sits above, so a headerless table never narrows the search) down to the page's bottommost anchor.
  **No printable operand row anywhere → v044's whole-page search, unweakened** — quotes the model composed
  rather than copied give no provable boundary, and the guard must not get weaker on doubt. Synonym matching
  itself (normalize_ws digit-gluing, substring per row) is byte-identical to v044's.
- **`backend/pipeline/test_confidence.py`** — the two MedCap end-to-end cases re-expectationed to what the
  scoped guard honestly produces on the real page text, plus three new cases (in-table bucket word, stored
  state, same-page two tables) — see Red/green. The v044 *direct* `_check` cases and XANO/Ework/Ependion
  expectations are untouched and stay green (the v044 direct cases carry no quotes in `fields`, so they
  exercise the page-level fallback path by design).

## Test cases (mid-file, after the v044/v052 block)

1. **MedCap, model left `due_within_1_year` null (v044's case, real pp.101–102 text)** — now FAILED with
   both nulls zero-filled: the carrying table names neither bucket (its first-year buckets are the two
   month rows, which no schema synonym names), so the cross-table `<1 år` hit that used to keep
   `due_within_1_year` at missing is gone *by design*, `0+616.5+0 != 718.7`, verified fields recap to 0.5.
2. **MedCap, fabricated-quote stub (v052's case)** — the drop still happens, the check now reads failed
   (not `missing: due_within_1_year`), so v052's above-window derivation — which only fires on a missing
   operand — no longer triggers on this page shape; `due_within_1_year` stays null@0.0.
3. **NEW, in-table bucket word** — the same maturity table with `inom 1 år` in its own header row: the guard
   keeps the null operand out, the check reads missing, v052's derivation fills 102.3 (value_derived, 1.0,
   joined label — every v052 assertion preserved) and the check now ends **passed**
   (102.3+616.5+0 = 718.8 ≈ 718.7) once `due_after_5_years` zero-fills.
4. **NEW, stored post-v052 state** — `due_within_1_year` = 102.3 with the derived two-row quote: the only
   null left is `due_after_5_years`, its `>5år` lives on the contractual table, so it is a confirmed 0 and
   the check **passes**. This is the work order's headline MedCap outcome.
5. **NEW, same-page two tables** — MedCap's two tables relocated onto one page, the bucket word printed
   below the operands' Totalt row: page-level rule (found → stay open) and the bottom-of-table boundary
   exercised at once → **passed**.

## Red → green (`cd backend && python -m pipeline.test_confidence`)

**Red**, new expectations run against the pre-v058 guard (`origin/acrylic`'s `extract.py` loaded into a
throwaway module, lane-temp script, not committed): the runner dies at the first re-expectation — MedCap
case 1 gets `'missing: due_after_5_years'` where the scoped zero-fill is required — and probes of the new
cases all stall at `missing: due_after_5_years` (stored state, in-table variant, same-page two tables).

**Green**, current code on the merged branch, full suite:

```
confidence self-check ok
```

Other gates: `test_kb` / `test_parse` / `test_paths` / `test_llm` all self-check ok;
`scripts/label_regression.py --schema income_statement --schema debt_maturity`: **identical=739 changed=0
skipped=173**; `git status --short data/` empty.

## Replay — 34 debt_maturity stems (seed1–4 kb, read-only) + 101 income_statement stems, zero model calls

Stored `fields` fed back as the `call_llm` stub, page text from each stem's own `pages.jsonl`, `pages`
recomputed with `locate.candidate_pages`; before = `origin/acrylic`, after = this branch. Every changed
stem verified by hand against its own pages (span rows enumerated, synonym hits located per page):

| stem | before → after | verdict |
|---|---|---|
| apotea | check missing → **passed** | `due_1_to_5_years`/`due_after_5_years` synonyms: in-span 0 (the p.114 hit is another note's table). The carrying table really has no 1–5y/>5y rows — v044's own evidence already said so. **Intended fix.** |
| storytel | check missing → **passed** | in-span 0; the report itself says the entire loan is classified as current, so 0+0 is true. **Correct pass.** |
| nelly | check missing → passed; `due_within_1_year` 287.1@0.5 → @1.0 (value_derived) | value unchanged (the model's own stored answer); the derivation now merely corroborates it. **Caveat, see Findings:** the pass rests on the model having answered the lease-note total as a bucket — v044's page-level rule accidentally masked this; pre-v044 behaved exactly like v058 here. |
| xano | check `missing: due_1_to_5_years` → `missing: due_after_5_years` | fields unchanged. Both buckets genuinely sit in the table's wrapped header; `Efter 5 år` is in-span (kept out, correctly), `Mellan 1/3` wrap apart so `due_1_to_5_years` is now a confirmed 0. The named missing just shifts; verdict stays open. |
| alligo | check missing → failed; two fields 1.0/0.9 → 0.5 | bucket words only on p.142's contractual cash-flow table; operands on p.143's carrying table. The sanctioned cross-table shape — same mechanism as MedCap's `>5år`. |
| boozt | check missing → failed; three fields 0.9 → 0.5 | p.108's *prior-year* lease table held the only synonym hits; the current year's table splits year 1 as month rows no synonym names — the MedCap-fresh class (see Findings). |
| bergman_beving | check missing → failed; two fields 1.0/0.9 → 0.5 | hits were p.119 (`after 5 years`) and p.123 (`Between 1 and 5 years`, five-year summary) — other pages/tables; in-span 0. |
| medcap | `due_within_1_year` 102.3@1.0 → null@0.0; check missing → failed | **The known fresh-extraction regression** — see Findings. |
| meko | check missing → failed; `due_within_1_year` 0.25 → 0.1 (loses arith_ok), total 0.95 → 0.5 | values unchanged. The field's quote is not on any page (pre-existing); with the check now numerically failing instead of missing, `arith_ok` is honestly withheld. |

income_statement: **101 stems, 0 differences** (the schema has no `null_as_zero` check — the guard never
fires; verified by running, not assumed). debt_maturity: 25/34 stems byte-identical.

## Findings

- **The work order's "MedCap → passed" holds for the stored/replayed state, not for a fresh extraction.**
  Scoping the guard removes the cross-table `<1 år` hit that was the *only* thing keeping
  `due_within_1_year` out of the zero set on MedCap's carrying table — its own bucket rows
  (`6 månader eller mindre`, `6 – 12 månader`) are printed right there but no schema synonym names them.
  Fresh extractions therefore zero-fill both nulls, the check reads failed (not missing), and v052's
  above-window derivation — triggered only by a missing operand — no longer fires: 102.3 is lost and the
  verified fields recap to 0.5. Reported to the supervisor mid-lane with probe evidence. **Root fix is a
  schema-vocabulary change, out of this lane's territory:** adding month-range synonyms
  (`6 månader eller mindre`, `6 – 12 månader`) to `due_within_1_year` restores the whole chain
  (guard keeps it out → missing → v052 derivation fills 102.3 → passed).
- **Nelly's new pass is technically permitted but semantically lucky** — the stored answer is the lease
  note's total, and the identity closes only because both buckets are confirmed 0s. A borrowings-maturity
  extraction for Nelly would want `due_1_to_5_years` from `Långfristiga`, which the schema deliberately
  does not synonym-map (v040). Flagged for the schema lane, not worked around here.
- The stored MedCap stub (joined two-row quote) takes a pre-existing detour: `_segment_column` votes the
  joined quote into another column (48), and the `value_derived` repair reads it back to 102.3. That
  detour predates this lane; on the pre-v058 code the repair was *suppressed* (the check read missing,
  which skips the repair loop), so the mangled 48 stuck — v058's passing check lets the pipeline self-heal.
- `_between_rows`/`_row_year_column`/`_stated_zero`/v054's retry entry points: only called, never modified.
- Not done: README (supervisor patches at merge, per the work order); no schema edits; no model calls.

## Contract

`docs/API.md` untouched. Response-visible differences are check `detail` strings and confidence caps for
checks whose null buckets' labels live outside the operands' own table — exactly the class this lane
exists to move from "missing forever" to a decided verdict.

## Supervisor ruling follow-up — the schema route (2026-09-17)

The supervisor ruled against the interim regression (fresh MedCap extraction losing the v052-derived
102.3): the fix follows this lane's recommendation — territory extended to
`backend/schemas/debt_maturity.json`'s `due_within_1_year.synonyms`, generic month-range wording only.
Target chain: table-scoped guard → the bucket rows are recognised (not zero-filled) → missing → v052's
derivation fills 102.3 → **passed**.

### What changed on top

- **`schemas/debt_maturity.json`** (its own `schema:` commit) — six synonyms, each verified present in
  seed1–4 maturity-table contexts: `6 månader eller mindre`, `6 – 12 månader` (MedCap p.101 rows),
  `less than 3 months` (catella p.133, ncab p.117), `0-3 months` (coor p.155, vef p.20),
  `less than 6 months` (arjo p.114), `6-12 months` (storytel p.114). `6 to 12 months` stays out: it
  appears only in forward-contract prose, never as bucket wording; `0–6 months` appears nowhere.
- **`extract.py`** — two amendments the replay forced:
  1. *Never worse than v044, by construction.* The table-scoped zero-fill is evaluated first; if it does
     not close the identity, the check falls back to v044's whole-page verdict (a zero set unchanged from
     page-level makes the fallback an identity). Rationale: a scoped zero that cannot close the identity
     has not proven its 0s enough to hard-fail the check and cap the verified fields — meko's baseline
     `missing: due_1_to_5_years` rested on p.101 lease-table hits; scoping alone turned it into a hard
     failure, which the ruling forbids.
  2. *The span needs a provable top.* `_operand_table_rows` now returns None unless a year-header row
     actually sits above the anchors on every anchored page — a page-top span proves no boundary (the
     real table may continue from the previous page: alligo/boozt/bergman_beving's maturity headers live
     one page up). A page-top span zero-filled buckets whose labels sit there, again turning missing into
     failed.
  3. *`_statement_row` honours its own "None when ambiguous".* With the month wording in the schema, two
     different full rows ("6 månader eller mindre", "6 – 12 månader") each match a different synonym of
     the SAME field — a finer split summing to the bucket. The exact-synonym path previously took the
     first hit and the statement-row fill claimed 54.3 as due_within_1_year, poisoning the derivation.
     Now: multiple exact hits → None, and the identity machinery reads the sum.
- **`test_confidence.py`** — the MedCap cases re-expectationed to the ruling's target chain: fresh null
  stub → missing → derivation fills 102.3 → **passed** (case 1); fabricated quote → same (case 2); v051's
  concatenation case now ends **passed** too. Stored-state/one-page/in-table variants document the
  confirmed-0 pass shapes.

### Red → green (ruling round)

Red: with the target expectations committed but the schema/guard unchanged, the suite dies at the first
MedCap assertion (`0 (due_within_1_year null) + 616.5 + 0` — zero-filled, `passed: False`). Green: full
suite on the final merged tree (includes v049/v051/v053–v057/v059/v060/v062):
`confidence self-check ok`; `test_kb`/`test_parse`/`test_paths`/`test_llm` all self-check ok.

### Replay (final, `scripts/replay_check.py`, baseline = origin/acrylic @ v062)

- **debt_maturity, seed1–4: 35 companies, 33 same, 2 changed — both improvements, none worse:**
  - `medcap_2025`: `due_within_1_year` None@0.0 → **102.3@1.0** (value_derived), check failed →
    **passed**; `total_debt` 0.5 → 0.9, `due_1_to_5_years` 0.5 → 1.0 (arith_ok restored). Verified: the
    `>5år` wording lives only on the contractual table; the carrying table's month rows are the new
    synonyms, so the null stays missing until the derivation closes it.
  - `storytel_2025`: check `missing: due_1_to_5_years` → **passed**, fields untouched. Verified: in-span
    synonym hits 0 for both null buckets, and the report's own sentence ("the entire loan classified as
    current") makes the zeros true.
  - meko/boozt/bergman_beving/alligo/xano/nelly/apotea: byte-identical to baseline (the fallback + the
    header gate hold every one of them at v044's verdict).
- **income_statement: 101 companies, 0 differences.**
- `label_regression`: identical=739 changed=0 skipped=173. `git status --short data/` empty.
- 7 stems not replayable ("no candidate pages derivable" — stored files carry no source pages or
  two_pass warning; equal on both sides, pre-existing tool limitation).

### Findings (ruling round)

- The statement-row fill was the hidden blocker in the target chain: with the month wording in the
  schema, `_statement_row` saw the split's FIRST row as an exact label match and filled 54.3 before any
  check ran. The uniqueness fix is a 3-line change to a function outside this lane's original territory —
  flagged here and in the delivery message; the alternative was failing the ruling's own chain.
- v051/v060/v062 landed on acrylic mid-lane (twice); both merges auto-resolved, and all gates were
  re-run on the newest tree each time.
