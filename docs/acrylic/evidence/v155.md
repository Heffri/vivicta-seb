# v155 — debt_maturity B bucket: nine non-empty wrong values

Base: `origin/acrylic` after the delivery-time merge. Scope stayed in
`backend/pipeline/extract.py`, `backend/pipeline/test_confidence.py`, and this
report; schemas, labels, locator, merge code and `data/` are unchanged. All
runs were offline with stored answers replayed as model input.

## Method

I read v150 plus v135, v148, v119, v103, v111, v052, v068 and v102, then
located both the label and persisted citation in `pages.jsonl`. I also
inspected the read-only UAW annual-report/table-shape area and acrylic theme.
It has no reusable Python citation-scope guard, so no UAW code was copied.

## Nine B rows

| Report / field | Label source | Persisted source | Diagnosis / action |
| --- | --- | --- | --- |
| better_collective / total_debt | **259691**, p161: `Debt to credit institutions 259,691 259,691 289,123 10,388 278,735 0` | **259946**, p161: `Debt to credit institutions 259,946 259,946 306,005 10,551 295,454 0` | **Interleaved FY2025/FY2024 blocks.** `2024` then `2025` headers sit beside alternating duplicate rows, so text order cannot map the second row safely. No guess/rule. |
| academedia / total_debt | **12103**, p87: `SUMMA 13 090 -2 460 220 30 1 222 12 103` under `FÖRÄNDRING FINANSIELLA SKULDER 2024/25` | **12114**, p87: `SUMMA 12 114 13 097` under the currency-by-borrowing summary | **Wrong generic-total subtable.** The current financial-liabilities roll-forward guard corrects **12114 → 12103**. |
| bergman_beving / total_debt | **2390**, p120: `Total interest-bearing liabilities 2,390 2,062` | **2038**, p119: `Interest-bearing financial liabilities bank 2,038 2,274 18 336 1,920 –` | **Lease scope.** Stored is bank-only; label Group summary includes IFRS16 leases (352). Needs scope ruling, not synthesis. |
| bergman_beving / due_within_1_year | **424**, p120: `Total current liabilities 424 476` | **354**, p119 bank row: `18 336` are `<3 months` plus `3 months–1 year` | Same bank-only vs Group-plus-IFRS16 scope ruling. |
| net_insight / due_after_5_years | **null**, p91: `>5 years - -` | **0**, same p91 row | **Label convention, not a bad read.** The standing printed-dash policy records an explicit nil as 0. No change. |
| tangen_industrikapital / total_debt | **588199**, p62: `Lån Kreditinstitut 141 734 137 898 251 081 57 486 588 199` | **915454**, p57: `Total upplåning (inkl leasingskulder) 915 454 530 245` | **Capital-management aggregate vs loan-only maturity row.** Stored explicitly includes leases; label excludes them. Needs debt/lease convention ruling. |
| dynavox / total_debt | **896.2**, p133: `Interest-bearing loans 24 896.2 896.2 691.5 691.5` | **893.8**, p135: `Consolidated statement of cash flows Interest-bearing borrowings 691.5 202.4 893.8` | **Cash-flow movement table.** Guard changes stale value to **null**; p133 was outside the stored candidate window, so no value is invented. |
| dustin / total_debt | **2538**, p115: `Liabilities to credit institutions 2,538 - 2,538 2,538` | **3055**, p117: `Total 4,188 -1,217 - 158 -101 27 3,055` | **Financing-activities movement total** combining credit institutions and leases. Guard changes it to **null**. |
| modern_times / due_within_1_year | **418**, p118: `Total interest-bearing liabilities 418 36` | **155**, p142: `Lease liabilities 155 37 33 85 151` | **Prior-year gross-value block.** The row follows `2024 Maturity ...` in `Terms and payback period, gross values`; numbers after it are maturity years, not fiscal columns. Guard changes it (and same stale p142 siblings) to **null**. |

Counts: wrong generic subtotal **1**; cash-flow/financing-movement table **2**;
prior gross-value block **1**; interleaved-year text shape **1**; lease/debt
scope decision **3** (the two Bergman rows plus Tången); dash-label convention
**1**.

## Code, red → green

1. `4fbd897 extract: select financial-liability roll-forward total`
   - Red real-page replay retained AcadeMedia's currency `12114`.
   - Green `test_financial_liabilities_rollforward_total` feeds a frozen real
     excerpt and asserts current-year **12103** plus its exact source row.

2. `5a865a6 extract: refuse cash-flow movement citations` and
   `59753d8 extract: refuse financing activity movement citations`
   - Red persisted Dynavox **893.8** and Dustin **3055** were page-verified.
   - Green `test_cash_flow_citation_refusal` rejects both real shapes and
     proves a standalone navigation/header mention does not reject an ordinary
     interest-bearing-loans row.

3. `1b27644 extract: refuse prior gross-value maturity citations`
   - Red MTG p142 retained **155** as FY2025 and the bucket-column fallback
     reintroduced it after citation rejection.
   - Green `test_gross_value_prior_year_citation_refusal` proves the nearest
     `2024 Maturity` block is refused while the preceding FY2025 block remains
     accepted; the bucket-column entry point shares the valve. The older frozen
     MTG test now proves a null cannot refill from that prior block.

## Verification

- `python -m pipeline.test_confidence`: green (`confidence self-check ok`).
- Final 18-store replay, both `debt_maturity` and `income_statement`, against
  `origin/acrylic`: **422 companies, 412 same, 10 changed, 444 not replayable**;
  values changed only in the intended stale copies: AcadeMedia `12114 →
  12103` (two copies), Dustin `3055 → null` (three), Dynavox `893.8 → null`
  (three), and Modern Times's p142 gross values → null (two copies). There
  are **0 value regressions**; income_statement is unchanged.
- `label_regression` over both schemas: `921 identical, 8 changed, 408
  skipped`. Its eight detailed predicate deltas are pre-existing False → True
  synonym-cleaning expansions; this lane changes no predicate: **0 True →
  False**.

## Re-publication recommendation

Re-run/re-publish **four reports** after merge: AcadeMedia gains the correct
12103. Dustin, Dynavox and Modern Times deliberately lose unsafe values and
need a fresh model/locator pass for their labelled values; null is safer than
high-confidence wrong values. Better Collective, Bergman & Beving and Tången
need the recorded table/scope ruling. Net Insight needs no change under the
printed-dash policy.

## Guardrails

No model API, network extraction, PDF download, KB registration, source-data
write or label write was run. The final owner-identifier scan is clean; this
report contains no machine path or copied command output.
