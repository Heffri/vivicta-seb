# v116 — targeted re-extraction: the 15 stems whose stored answers were known wrong or empty, re-run
# through the current pipeline (Codex terra, two-pass) so `data/kb` finally holds the correct explicit
# values — 9/15 companies land on their labels, 27/36 label fields ✓; the wrong-table guards refused
# only wrong tables (17 named-table refusals, 0 correct-table losses); every remaining miss is a
# locator miss (5) or a model decline (1), none a mechanism refusal

Lane `ao/vivicta-glm-58/v116-reextract`, off `origin/acrylic` (`be1449f` at dispatch). **Zero code
changes** — `backend/`, `frontend/`, `eval/`, `scripts/` byte-identical to the baseline; this lane
runs the pipeline as committed and measures what the v095/v103/v109/v110/v111 mechanisms do on the
fifteen stems whose stored answers v103/v105/v108/v111 documented as wrong or null. Delivery merge:
`origin/acrylic` had moved to `55d0fb3` (v112's KB collection switch — frontend/desktop/docs only,
`git diff be1449f..55d0fb3 -- backend/` is empty), merged before push; extraction semantics are
therefore identical at dispatch and delivery. Backend on PyMuPDF **1.27.2.3**, `PARSER_VERSION = 6`,
`LLM_PROVIDER=codex`, `LLM_MODEL=gpt-5.6-terra`, `EXTRACT_TWO_PASS=1`, `EXTRACT_QUOTE_RETRY` unset,
`LLM_BASE_URL` unset, `DEBT_BASIS` unset (**carrying**; `GET /api/config` → `"llm":true,
"provider":"codex","maturity_basis":"carrying"`).

## Method

The 15 PDFs were copied from the shared lane library into this worktree's `data/reports/` and
registered with `POST /api/reports/from-library` (`data/reports/index.json` extended locally with
the 15 entries, restored after the run; the PDFs are gitignored). Each stem was extracted **twice**
(`pass1`/`pass2`, two independent routes, the round's stability rule); every response is preserved
verbatim in [`v116/`](v116/) as `<stem>.pass{n}.debt_maturity.json` (30 files). The stored
`data/kb/<stem>/extractions/debt_maturity.json` in this worktree now holds each pass2 result —
left uncommitted for the supervisor to collect into `seed-rex-kb` and publish with
`publish_kb.py --accept-loss`. Driver rule (v082–v108 method, LESSONS 37): before each call the
evidence file's existence is checked (skip, never resend); on a timeout the driver polls the
server-side save instead of re-sending — neither path was needed (all 30 routes answered, 20–114 s).
Backend `[extract]` log lines: **30** — the round's own call basis; the widen fallback fired 0
times and no main call timed out, so ≈60 actual model invocations, exactly the work order's cap
estimate (30 lines × 2). `provider-usage.py`: codex seven-day **79% → 81%** (resets 09-20 12:45).

## Results — 15 companies × 2 passes

| Company | Two-pass pick (p1 → p2) | Fields (best pass) | vs label |
|---|---|---|---|
| RVRC Holding | `[109,110]` both | total **12**@0.9 (p.109 Note 21); w1y/1-5y/>5y null | **3/4** — total ✓; w1y 5 missing (BS p.95, not a candidate) |
| Lime Technologies | `[72,73]` both | all null | **2/4** — the two null-label buckets ✓; total 211,300 / w1y 80,786 missing (Note 21 p.91, not a candidate) |
| Smartcraft | `[59,60]` both | all null | **1/4** — only the null-label >5y ✓; the three carrying values missing (lease roll-forward p.58, not a candidate) |
| BioInvent | `[65,66]` both | p1 total **8,527**@0.9; p2 + w1y **7,370**@0.9 (p.66) | **3/4** (best pass2) — 1-5y 1,157 missing: the row prints on p.66, which the model read for two other fields, and it still declined |
| Volati | `[177,178]` both | total **3,246**@0.9, w1y **192**@0.25 (p.177); 1-5y/>5y null | **1/1 ✓** — the acceptance w1y 192, the model's own answer kept |
| Salix | `[177,178]` both | p1 total **3,246**@0.9, w1y **192**@0.25; p2 w1y null | **2/2 ✓** (best pass1) |
| Momentum | `[116,117]` both | p2 total **622**@0.9 (p.117), buckets null | **4/4 ✓** (best pass2) — p1's model-answered 161/11 (BS rows read as buckets) are the one instability; p2 is the honest null pass |
| Dynavox | `[135,136]` → `[134,135]` | total **893.8**@0.9 (p.135 cash-flow row) | **0/1 ✗** — label 896.2 (amortized-cost row p.133); 133 was never a candidate |
| Inwido | `[143,144]` both | p1 total **1,796**@0.9 (p.143 prose); p2 null | **1/1 ✓** (best pass1) |
| Attendo | `[102,103]` → `[103,104]` | total **2,978**@0.9 both (p.102/103) | **1/1 ✓** — value ✓ (label page 101; read one page over) |
| BHG | `[134,135]` both | all null | **0/1 ✗** — label 1,297.4 (`Loans from banks`, p.128); not a candidate ([150,151,134,97,102,147]) |
| Humble | `[90,91]` both | total **1,668**@0.9 both (p.90 Book value column) | **1/1 ✓** |
| Karnell | `[106,107]` both | **397.2 / 43.5 / 353.7 / 0**, all @0.9–1.0, check **passed** | **4/4 ✓** — the >5y 0 is v095's off-grid derived zero, live (`>3 years` dash + closure) |
| Ambea | `[119,120]` both | total **12,643**@1.0, w1y **2,308**@0.9 both (p.119) | **2/2 ✓** — the 2025-first column read correctly both passes, no year-flip anywhere |
| NOTE | `[107,108]` both | total **824,281**@1.0, w1y **595,420**@0.9 both (p.107) | **2/2 ✓** — v110's subtotal-pair fill, live: the model returned null, the mechanism summed 228,861 + 595,420 with each closing on its own rows |

**Companies at full label agreement: 9/15** (Volati, Salix, Momentum, Inwido, Attendo, Humble,
Karnell, Ambea, NOTE); **label fields: 27/36**. Side metric, valued fields whose label row pins a
page: 14/17 page hits (misses: Dynavox 135≠133, Inwido 143≠166 prose, Attendo 102≠101). Every one
of the work order's 重点验收 lines except the four locator misses is now produced by the pipeline:
RVRC 12✓/5✗, Volati/Salix w1y **192**✓✓, Momentum **622**/null ✓, Ambea 12,643/2,308 ✓, Karnell
397.2/43.5/353.7/0 ✓, NOTE 824,281/595,420 ✓, Inwido 1,796 ✓, Attendo 2,978 ✓, Humble 1,668 ✓;
Lime, Smartcraft, Dynavox, BHG remain value-misses.

Stability: value-identical pass1==pass2 **11/15** (BioInvent, Salix, Momentum, Inwido differ —
in every case one pass holds the label-correct read and the other declines); page-pick identical
13/15 (Dynavox and Attendo moved one page, values stable).

## The guards' scorecard — the round's headline

**17 refusal warnings across 7 companies, and every single one names a genuinely wrong table**;
no mechanism refused a correct value anywhere in the 30 routes:

| Company | Refused reads (warnings name the table) | What survived instead |
|---|---|---|
| RVRC ×4 | p.110 `Maturity analysis regarding non-discounted liabilities` Total rows (210…220 and 225…235) — v103 | Note 21's carrying 12 (the stored 220/210/10/0 is gone) |
| Lime ×2 | p.72 `Liquidity risk - Group` `Borrowing (incl. overdraft)` column read — v103 | honest nulls (the carrying note was never offered) |
| Smartcraft ×4 | p.59 `Total undiscounted lease liabilities` fills — v103 | honest nulls |
| Volati ×2, Salix ×2+1 | NOTE 19 `Timing of revenue recognition, contract liabilities` `Within 1 year 87` fill — **v111 non-debt-subject** | **the model's own 192 kept** (0.25, quote-not-found) — the guard's keep-effect is the value win |
| Momentum ×3 | p.111 `Parent Company`-section lease-table fills — **v111 parent-section** | clean null buckets beside the Group's 622 |
| NOTE ×2 | the model's own 824.2 citation from the `Likviditetsrisker` prose block — v103 (undiscounted) | v110's subtotal-pair 824,281 — the refusal and the fill composing correctly |

This is the first live round on these stems since the guards landed: v103's
undiscounted/all-liabilities refusals, v111's non-debt-subject and parent-section refusals, v110's
subtotal pair and v095's off-grid zero all fired on real pages, in combination, with zero correct-table
collateral — the exact behaviour their evidence claimed, now demonstrated on the extraction path
rather than by replay.

## Mechanism hit table (best pass; a dash = no attributable marker in the saved JSONs)

| Company | two-pass | v103/v111 guard refusal | v110 subtotal pair | v095 off-grid zero | value_derived | computed-not-read drop | quote-not-found | v101/v102/v096/v104/v109/by_year |
|---|---|---|---|---|---|---|---|---|
| RVRC | ✓ | ✓ (×2/p1, ×2/p2) | – | – | – | – | – | – |
| Lime | ✓ | ✓ (×1 each pass) | – | – | – | – | – | – |
| Smartcraft | ✓ | ✓ (×2 each pass) | – | – | – | – | – | – |
| BioInvent | ✓ | – | – | – | – | – | – | – |
| Volati | ✓ | ✓ (the 87 fill) | – | – | – | – | ✓ (192 @0.25) | – |
| Salix | ✓ | ✓ (the 87 fill) | – | – | – | – | ✓ (192 @0.25, p1) | – |
| Momentum | ✓ | ✓ (×3, p2) | – | – | – | ✓ (450, p1) | – | – |
| Dynavox | ✓ | – | – | – | – | – | – | – |
| Inwido | ✓ | – | – | – | – | – | – | – |
| Attendo | ✓ | – | – | – | – | – | – | – |
| BHG | ✓ | – | – | – | – | – | – | – |
| Humble | ✓ | – | – | – | – | – | – | – |
| Karnell | ✓ | – | – | ✓ (`>3 years` dash → 0@1.0) | ✓ | – | – | – |
| Ambea | ✓ | – | – | – | – | – | – | – (2025-first column needed no correction) |
| NOTE | ✓ | ✓ (the 824.2 prose citation) | ✓ (228,861 + 595,420) | – | ✓ | – | – | – |

Tally: two-pass 15/15, 0 fallbacks; named-table refusals 7 companies / 17 warnings (0 misfires);
v110 1; v095 1; v101/v102/v096/v104/v109 0 (no off-vocabulary shapes occurred in this set);
stated_zero / printed_nil / identity_kept 0; sign flips 0.

## Gap classification (the work order's per-company demand — why the 6 companies are not ✓)

1. **Locator — 5**: the correct page was never among `locate.candidate_pages`, so no fill or
   citation could reach it: **RVRC** w1y 5 (Group BS `Lease liabilities 18 5`, p.95; candidates
   [110,111,109]), **Lime** 211,300/80,786 (Note 21, p.91; candidates [72,73]), **Smartcraft**
   28,248/13,439/14,809 (lease roll-forward + BS, p.58; candidates [59,60,31]), **Dynavox** 896.2
   (amortized-cost reconciliation, p.133; candidates [134,135] — the model then cited the cash-flow
   893.8 it was handed), **BHG** 1,297.4 (`Loans from banks`, p.128; candidates
   [150,151,134,97,102,147] — the model declined everything it was offered).
2. **Model — 1**: **BioInvent** 1-5y 1,157 — the `Long term 1,157` row prints on p.66, a candidate
   the model read `Short term 7,370` and the 8,527 total from, yet it declined the third field in
   both passes.
3. **Mechanism — 0**: no correct value was refused, dropped or overridden anywhere in the round;
   every refusal warning names a table the labels themselves reject.

## Gates and housekeeping

- `git diff origin/acrylic --stat` (after the 55d0fb3 merge): `docs/acrylic/evidence/v116.md` +
  `docs/acrylic/evidence/v116/*.json` (31 files) — **no backend, frontend, schema, eval or scripts
  paths**; `backend/` is byte-identical to both the dispatch and delivery baselines.
- Owner-identifier grep over every changed file vs baseline: empty (checked before staging, push
  run separately). The 30 evidence JSONs contain no codex error text and no machine paths (checked
  by direct grep before staging).
- `data/kb/<stem>/extractions/debt_maturity.json` for the 15 stems modified in the working tree and
  **left uncommitted** for the supervisor (`seed-rex-kb` → `publish_kb.py --accept-loss`);
  `data/reports/index.json` restored after the run; the 15 copied PDFs stay in `data/reports/`
  (gitignored). `register_library`'s one-time meta/pages rewrites (LESSONS 27) ride in the same
  uncommitted set.
- Scratch (driver, compare script, backend log, 30-route timing) lives in the lane temp dir
  outside the repo.

## For the group (what a future lane should pick up)

1. **The residual is the locator.** 5 of 6 remaining misses are `candidate_pages` never offering
   the page the labels pin — lease roll-forwards, balance-sheet rows, credit-institution notes and
   reconciliations for exactly the report shapes this set was chosen for. The value layer is
   demonstrably ready (NOTE/Karnell/Volati/Salix land the moment their pages are in scope); widening
   the debt_maturity candidate pool with balance-sheet/roll-forward neighbours, or letting the
   two-pass page-ask see the statement spread, is the highest-value lever left.
2. **The quote-not-found 0.25 family gains a member**: Volati/Salix's 192 is value-correct and
   kept only because the overriding fill is refused — the model's joined quote never verifies
   (0.25 cap), the BTS-family shape on a carrying note.
3. **BioInvent's partial decline** (page in hand, one field answered) is the one pure model
   variance; two-pass disagreement on the same stem (4/15) remains the honesty metric for it.
