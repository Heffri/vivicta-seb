# v154 — the D bucket (label candidates) audited row by row on the real pages: 9 rows / 8 companies

Lane `ao/vivicta-glm-89/v154-label-audit-D-bucket`, off `origin/acrylic` (`ccf4c2f` at dispatch).
Territory: `eval/labels.csv` (this audit touches 1 row, notes only — zero value/page changes) and
this file. No code, no `data/`, zero model calls, zero API calls. Every verdict below was read by
me from the real page text (`data/kb/<stem>/pages.jsonl`) and checked against the CURRENT stored
extraction (`data/kb/<stem>/extractions/debt_maturity.json`) — the same records v150's breakdown
bucketed as D. Method: read `common.md`, `reporting.md`, `LESSONS.md` (44), the v150 D rows,
`v149.md` (D definition), `v135.md` + `v136.md` (the volati 3246-vs-3245 / green 2491-vs-2495
report-internal records), `v126.md` (Stillfront), `v128.md` + `v078.md` (dash-to-0), then the
pages.

## Verdicts: L 0 / I 2 / P 7 / U 0

- **L (label wrong) — none.** Every label value survived its page: each is either a printed
  figure, the printed subtotal pair, or a sum of printed rows the schema's finer-split rule
  sanctions, with the row's/note's own identity closing exactly.
- **I (report self-inconsistent) — 2:** green_landscaping total_debt, volati total_debt. Both keep
  the v135 ruling: the label takes the note's own closing rows; the notes record where the other
  number prints.
- **P (label right, stored wrong — for the mechanism) — 7:** listed with the mechanism gap each.
- **U — none.** Nothing was missing; every row closed on its page.

## The 9 rows

### 1. `byggmastare_a_j_ahlstrom_h_2025` / `due_1_to_5_years` 1911 @ 54 — **P**

p.54 prints the 2025 amortisation ladder under `Redovisade leaseskulder 31 december 2 873`:
`Amotering år 1 962` (853), `Amotering år 2 1054` (1 002), `Amotering år 3 857` (1 082),
`Amotering år 4 - 0` (868). 1054 + 857 = 1,911; the identity 962 + 1,911 + 0 = 2,873 closes
exactly. Stored: null, warning `1911 is printed on none of pages [53, 54]; dropped as computed,
not read` — the guard drops a sum of the row's own printed cells. Mechanism note: the summand
wording is Swedish amortisation-year rows (`Amotering år 2/3`), which sit outside the v126 window
grammar (English `Repayment … yr.`), so no rescue path exists for this shape today. (The same
row's `due_after_5_years` is labeled 0 per the dash ruling and its row closes — correct, not in
the D bucket.)

### 2. `green_landscaping_2025` / `total_debt` 2495 @ 76 — **I**

BS p.76 prints `Liabilities to credit institutions` 1,908 (non-current) + 87 (current) +
`Bonds 500` = 2,495; leases are separate BS lines (454 + 212 = 666, not added per the report's own
classification). Note 24's own rows close on the same number: 87 + 2,398 + 10 = 2,495. The
report's other number: **Note 33's cash-flow reconciliation closing row, p.103** —
`Closing balance 2025-12-31 2,491 666 3,157` (2,491 credit institutions + bond loan, 666
leasing, 3,157 total) — 4 off the BS. The label keeps 2,495 (the note's own rows), the same
treatment as volati; the notes now record where 2,491 prints. Correction for the record: v136
called 2,491 "note 24's printed closing balance"; the audit locates it precisely in Note 33
(p.103) — the number was right, the note attribution was not. Stored: null (the stored run
selected pages [99, 100] and answered nothing).

### 3. `green_landscaping_2025` / `due_1_to_5_years` 2398 @ 102 — **P**

Note 24 (p.102) prints `Liabilities to credit institutions maturing between 1 and 5 years 1,898`
and `The bond loan matures within 1–5 years 500`; 1,898 + 500 = 2,398, and 87 + 2,398 + 10 =
2,495 closes exactly. Stored: all four fields null — the sum of the note's two printed rows is
provable but was never produced. Mechanism note: same computed-sum family as row 1, and there is
no model value to rescue (the stored passes returned null).

### 4. `mildef_2025` / `due_within_1_year` 137.7 @ 112 — **P**

Note 22's short-term section prints `Liabilities to credit institutions 98.0` +
`Lease liabilities 39.7` (contingent `–`); no printed short-term subtotal; the note's own total
`Total borrowing 772.9` closes with the dash read as 0 (rows sum 772.8; printed total wins).
98.0 + 39.7 = 137.7. Stored: total_debt 772.9 ✓ but due_within_1_year null. Label right; the
section-row sum is unproduced.

### 5. `proact_it_2025` / `due_within_1_year` 312458 @ 103 — **P**

The Group 2025 maturity table prints `Bank loan, Svensk Exportkredit … 16 Jul 2026 216,360` +
`Lease liability … 2026 96,098` = 312,458; 312,458 + 166,153 (2027–2030) = 478,611 = the printed
`Total interest-bearing liabilities` — closes exactly. The carrying note corroborates:
`Bank loans, of which short-term portion 216,360` and the prose "short-term loans that fall due
in 2026 amount to SEK 216 360 thousand". Stored: **0** @ 103, warning `312458 is another
segment's column; the page's rows are read from column 1 of 3, which prints 0` — the column read
took the undrawn rows (`Utilised overdraft facility, Nordea -`, `Bank loan, Nordea -` ×2;
footnote: utilised SEK 0 thousand) instead of the 2026-maturity rows. A wrong-column/wrong-row
read, not a report inconsistency. Label right.

### 6. `sdiptech_2025` / `total_debt` 4495 @ 110 — **P**

Note 24 prints both section totals: non-current `Total 4,049` (2,309 + 800 + 342 + 597 + 2) and
current `Total 446` (10 + 119 + 317 + 1); 4,049 + 446 = 4,495 — the v110/v131 subtotal-pair
shape. Stored: total null while due_within_1_year 446 ✓ is stored, so the page was selected and
read — only the sum of the two printed totals was never produced. The same page's contractual
`Repayment periods` table (770/239/4,314/222) stays declined (undiscounted, per the label notes).

### 7. `stillfront_2025` / `due_within_1_year` 710 @ 109 — **P**

Note 21's maturity structure prints the current-classified rows `Current liability 675`
(contingent considerations) + `Current liability 35` (other non-current liabilities and
non-current lease liabilities) = 710; the bond and credit-institution current rows print `–` and
ride at 0 (v126's hand verification). Stored: null, warning `732 is printed on none of pages
[109, 110]; dropped as computed, not read` — the model's own current sum was 732 = 710 + 22: it
added `Other interest-bearing liabilities 22`, a row with no repayment-timing split anywhere on
the page (v126's finding). So the computed-not-read guard fired, and the v126 window-sum rescue
could not fire either — it requires the model's own value to equal the window family's sum, and
732 ≠ 710. The honest 710 is provable (675 + 35) but unproduced. Note the same stored extraction
now holds `due_1_to_5_years` 5,152 via the v126 rescue (`kept as value_derived`), so the
mechanism works when the model's sum matches the family — this row's failure is the 22-row
inclusion, not the rescue.

### 8. `tangen_industrikapital_2025` / `due_1_to_5_years` 388979 @ 62 — **P**

p.62's balance-sheet-linked maturity table prints `Lån Kreditinstitut 141 734 137 898 251 081
57 486 588 199`: "Mellan 1 och 2 år" 137,898 + "Mellan 3 och 5 år" 251,081 = 388,979 (finer-split
sum per the schema rule); the identity 141,734 + 388,979 + 57,486 = 588,199 closes exactly.
Stored: null — the stored run selected pages [63, 64] and took 915,454 from p.63's `Total
upplåning (inkl leasingskulder)` (that total is the separate B-bucket dispute on this company,
outside these 9 rows; the p.62 table was never read for buckets). Label right.

### 9. `volati_2025` / `total_debt` 3245 @ 177 — **I**

Note 17 prints both section subtotals on p.177: `3,053` (leases 375 + credit institutions 2,678)
+ `192` (overdraft `–` + 12 + 180) = 3,245; no printed grand total. Note 18's roll-forward prints
`31 December 3,246` — a 1-unit report-internal disagreement. The label keeps note 17's own
subtotal pair (v135 Finding 4, image-verified there); the stored 3,246 scores value ✗ / page ✓ —
kept exactly as v135 ruled ("worth keeping exactly as it is"). The notes already record it
(`note 18 roll-forward prints 3,246, 1 off (report-internal)`) — no change needed.

## Dash-bucket sweep (v078/v128/ctt ruling)

No dash-bucket label among the 9 rows or the 8 companies needs a flip. The only dash-bucket label
in the group, byggmastare `due_after_5_years` = 0, closes on its own row (962 + 1,911 + 0 =
2,873) and already follows the ruling. Everywhere else the labeled buckets print values
(green 10, tangen 57,486, stillfront 4) or the report prints no carrying bucket rows at all
(sdiptech 1-5y/>5y and mildef buckets: their only maturity tables are contractual/undiscounted,
declined; volati has none), so null-lack-of-label is correct, and dashes inside summed sections
(mildef contingent `–`, volati overdraft `–`, stillfront `–` current rows) ride at 0 inside
closures that hold.

## Gates

- `python eval/run.py --stored-kb data/kb --no-fail` — before the edit: **313/367 (85.3%)**
  value, **254/313 (81.2%)** page; after the edit: **byte-identical full output** (the one
  labels.csv change is notes-only; notes do not enter scoring).
- `python scripts/label_regression.py --schema income_statement --schema debt_maturity --baseline
  origin/acrylic`: totals **identical=921, changed=8, skipped=408; True→False = 0**. The 8 are
  the documented pre-existing first-scoring asymmetry set (duni, enea, medcap×2, nobia×2,
  rejlers, rusta — v126's recorded set, all False→True, evaluated on stored fields), and none of
  the 9 audited rows appears (green_landscaping: identical=0, changed=0, skipped=4).
- `git diff --stat` vs baseline: `eval/labels.csv` 1 line, this file. Owner-identifier grep over
  every changed/new file: empty (run before staging, checked, then pushed).
- Scratch scripts and page dumps live in the lane temp dir outside the repo (not committed).
