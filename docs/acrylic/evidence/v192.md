# v192 — README rows for the 09-21 evening / 09-22 lanes

Docs-only: `docs/acrylic/README.md` is the only tracked file changed besides this evidence. No
`backend/`, `frontend/`, `schemas/`, `eval/` or `data/` byte touched, zero model calls, no servers
started. Baseline `origin/acrylic` `4b95dc0` (re-fetched at delivery — unchanged).

## Rows added to `docs/acrylic/README.md` (7 lanes), by section

- **Frontend table** (2 rows, appended after the v180 maturity-wall row, numeric order):
  [v182](v182.md) (Review — statement-first: one card per report/statement, basis suggestions with
  their source, the four groupings, Next unresolved field; landed merged on Sebastijan's #7 basis
  semantics), [v187](v187.md) (Extract — batch runs genuinely three-wide, plus the merged tree's
  6 red e2e cases repaired; full suite 59/0).
- **Backend hardening table** (5 rows, appended after Sebastijan's v185 sprint row, numeric order):
  [v181](v181.md) (structured `missing_reason` for final-null debt fields — "Why empty" in Results
  and the PPTX field table), [v183](v183.md) (an absent `reports/index.json` reads as an empty
  library — registration no longer plaintext-500s on an empty data directory),
  [v185-heldout-attribution](v185-heldout-attribution.md) (the held-out round's 9 non-empty wrong
  values attributed offline across four named guard families — the row points at the evidence file
  explicitly, since [v185](v185.md) is Sebastijan's #7 sprint), [v186](v186.md) (parent-note continuation +
  lease-only schedule guards: 7/9 held-out wrong values → null, stored-library replay 0 changed),
  [v189](v189.md) (ESEF/iXBRL coverage probe: no FY2025 filings, no portable total-debt tags —
  specified, not implemented).
- **What a colleague sees now** (v178 bullet relabelled per the work order): the held-out Small Cap
  first extraction is now "round 1", with a pointer to its 9 wrong values being attributed in
  v185-heldout-attribution and guarded in v186.
- **How to verify**: the e2e line now says **59 tests** (v187's merged-tree count; backend still
  15 standalone suites).

## Gates

- `git diff --name-only origin/acrylic` → exactly the two files above.
- Owner-identifier grep over the changed files (run as its own command, before any push) → clean.
