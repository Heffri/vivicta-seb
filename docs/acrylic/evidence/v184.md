# v184 — README rows for the 09-19 → 09-21 lanes

Docs-only: `docs/acrylic/README.md` is the only tracked file changed besides this evidence. No
`backend/`, `frontend/`, `schemas/`, `eval/` or `data/` byte touched, zero model calls, no servers
started. Baseline `origin/acrylic` `6d4acc5` (re-fetched at delivery — unchanged).

## Rows added to `docs/acrylic/README.md` (18 lanes), by section

- **Frontend table** (9 rows, appended after the v112 collection-switch row, numeric order):
  [v163](v163.md) (Knowledge base — export everything: whole-collection CSV + PPTX deck),
  [v164](v164.md) (Extract — candidates during the wait, the nothing-found banner, the one-click
  real-debt sample), [v169](v169.md) (Knowledge base — the SEB Mid Cap collection),
  [v171](v171.md) (Extract — batch runs surviving tab switches),
  [v172](v172.md) (Review — corrections with their own page/quote/component evidence),
  [v174](v174.md) (Compare — the upcoming-maturities wall),
  [v175](v175.md) (Knowledge map / Review queue following the collection picker),
  [v179](v179.md) (Results — citation line located on the page image and in saved text, OCR marked),
  [v180](v180.md) (Knowledge base — maturity wall by sector: KB card + deck pages).
- **Desktop app** (1 paragraph after the v107/v138 data-directory sentence): [v176](v176.md) — the
  shared unpacked test package refreshed to the tip (41 s warm full refresh, `BUILD.txt` naming the
  merge commit) and the clean-userData closed loop with the measured walkthrough times.
- **Backend hardening table** (7 rows, appended after v158, numeric order): [v162](v162.md)
  (`PAGE_SELECT_HINTS=retry` measured no benefit, default stays off), [v165](v165.md)
  (`absent_in_table` — "Not printed in this report"), [v166](v166.md) (the `bs_tie` balance-sheet
  tie-out), [v168](v168.md) (merge votes must be the same financial question),
  [v170](v170.md) (note-number locator: measured, not shipped — ceiling 1 company),
  [v173](v173.md) (`GET /api/kb` warm 940 → 117 ms per-stem fingerprint cache),
  [v177](v177.md) (analyst-pinned one-field fill; controlled experiment 0/8).
- **Team collaboration** (new subsection "For whoever presents: the demo pack and the honest
  numbers", directly after the 09-18 merge section): [v167](v167.md) — `docs/DEMO.md`, the repo
  README first screen, the re-measured two-claim accuracy framing, the `PERFORMANCE.md` stamp.
- **How to verify**: the backend block now lists all **15 standalone suites** (12 under `pipeline/`
  plus `test_collection`, `test_human_review`, `test_workbench` — locations and one-line purposes
  verified against the tree, not from memory) and the e2e line says **52 tests**.

## v178 row — position check (per the work order, no change needed)

The existing [v178](v178.md) row sits in "What a colleague sees now" as the "Held-out Small Cap
first extraction" bullet of the Accuracy claims, directly after the "First extraction (no labels at
run time), the three measured batches" bullet. That is the right place: v178 is a first-extraction
measurement with blind labels, kept separate from the curated stored-library claim, and its
"Small n, no market extrapolation" guardrail sentence is already there. Left untouched.

## Gates

- `git diff --name-only origin/acrylic` → exactly the two files above.
- Owner-identifier grep over the changed files (run as its own command, before any push) → clean.
