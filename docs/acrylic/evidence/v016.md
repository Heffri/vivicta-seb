# v016 — team notes: evidence

Territory: `docs/acrylic/README.md` (new, 156 lines) and this file. No code touched, no screenshots
taken — every image linked from the new README already existed in `evidence/v010/`.

## What was written

`docs/acrylic/README.md` — an English handoff doc for Sebastijan / Boyu / Sara, structured exactly per
the work order's six sections: what the branch is + how to run it (fixture mode, `data/reports/`,
tone toggle, one line pointing to `frontend/README.md` for the dev server instead of documenting
screenshot tooling); a frontend view-by-view table + screenshot gallery (linked to `evidence/v010/`,
7 tabs × dark/light) + new `ui/` primitives + what's unchanged; a backend table (locator TOC,
null-bucket identity check, three label-normalization fixes, parser table-row merge) with a numbers
paragraph underneath; six contract gaps; two open questions for Kristian/SEB; three verify commands
plus usage lines for the three offline scripts.

## Facts checked (not just copied from evidence prose)

- Read all required source docs first: root `README.md`, `CONTRIBUTING.md`, `docs/HANDOFF.md`,
  `frontend/README.md`, `docs/acrylic/DESIGN.md`, and all 13 evidence files present at baseline
  (`v001`–`v006`, `v008`–`v014`; there is no `v007`, and `v011b` lives inside `v011.md` as a second
  heading, not a separate file).
- Verified the "new ui primitives" claim against the actual files rather than trusting the work order's
  phrasing: `frontend/src/components/ui/{input,segmented,state}.tsx` all exist; `badge.tsx` has
  `success`/`warning`/`danger` variants (grepped); `table.tsx` has the `containerClassName` prop
  (grepped). `input.tsx`/`segmented.tsx`'s own header comments date the promotion to v010, which the
  v010 evidence prose doesn't mention directly — cross-checked against `git log` on `frontend/`
  (commit `7d067a9`, inside v010's merged range) to confirm it's real and not invented.
  `frontend/src/api.ts` / `types.ts` last changed at `cd6ebe1`, before the branch existed — confirms
  every lane's "logic unchanged" claim at the git-history level, not just by trusting the prose.
  `scripts/{locate_check,label_regression,parse_check}.py` usage lines in the README are copied
  verbatim from each script's own module docstring, not reconstructed from memory.
- The work order's frontend-table instruction named `v001–v010/v015` as source material; `v015` does
  not exist in this repo (`docs/acrylic/evidence/v015.md` absent, no `v015` branch/commit anywhere in
  `git log --all`), confirmed twice — once at the start of this session and once more via
  `git fetch origin && git log HEAD..origin/acrylic` right before writing the table, both times HEAD
  == `origin/acrylic` @ `64b2c83`. Not a blocker: the work order's own reading list already scoped the
  material to `v001`–`v014`, so the table is built from the 10 frontend-relevant files that exist
  (`v001`–`v006`, `v009`, `v010`) with no gap in what teammates need to know.
- The six contract gaps and two open questions are the exact set the work order enumerated — traced
  each back to its source file (`v001`/`v003`/`v004`/`v005`/`v006`/`v009`/`v010`/`v011`/`v014`) rather
  than re-deriving them from a fresh read of "found but out of scope" notes, so nothing was added or
  dropped from that list.
- Every number quoted in the backend section (TOC hit counts, the two reverted-experiment tallies, the
  735/736 label regression, the 12.14%/20.42% → 0.24%/0.19% split rates, 76/76 + 3/3 quotes) was
  cross-read against its source evidence file's own numbers table, not paraphrased from a summary
  sentence elsewhere in the same file.
- No uncertain facts were left as "(see evidence/vNNN.md)" placeholders — everything referenced was
  directly verifiable either in the evidence files or in the current repo state.

## Gates

- No code changed; no build/lint/test gate applies to this lane.
- `git diff --name-only origin/acrylic -- docs/acrylic/README.md docs/acrylic/evidence/v016.md | xargs grep -il <owner identifier>` → empty (owner-identifier check).
- Every relative link in the new README (5 background docs, 13 evidence-file links, 14 `evidence/v010/*.png` screenshot links) resolved against the working tree before commit.
