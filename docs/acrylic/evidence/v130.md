# v130 —— `python -m pipeline.test_fetch` 用例 20 改吃冻结 fixture(`data/kb/modern_times_2025` 已重建为 FY2025)

基线 `origin/acrylic`(cda9766;交付前已 fetch,acrylic 未再前进)。领地:`backend/pipeline/test_fetch.py`、
`backend/fixtures/mtg_fy2021_cover_pages.jsonl`(新)、本文件。fetch.py、data/、其它测试未动。零模型调用。

## 前提核实

- 8a0e784 把 `data/kb/modern_times_2025` 整条重建为 FY2025 全本(184 页,meta sha `9fa80cea…`);
  现条目第 1 页文本 `Annual and Sustainability \nreport 2025…` —— 封面已含 2025,锚定年份检查放行,
  用例 20 的"必须拒绝"断言落空。
- 重建前条目(8a0e784~1,即 4590a72 之子链上的 FY2021 版)是 148 页,`pages.jsonl` 前 3 行第 1 页为
  MTG 的"年份在标题上方"版式 `2021 / ANNUAL AND / CORPORATE / RESPONSIBILITY / REPORT`。

## 红绿(两次观察分开记)

- **红**(基线,`python -m pipeline.test_fetch`,exit 1):
  `http://127.0.0.1:<port>/mtg-annual-and-cr-report-2021.pdf -> ok (48 pages, 0s)`
  (旧接受症状:FY2021 封面 + 前瞻 2025 填充的合成 PDF 被放行),随后
  `AssertionError: expected LookupError: the FY2021 document must not pass the anchored year check`
  (test_fetch.py 用例 20,当时 408 行)。
- **绿**(改后全套,exit 0):
  `http://127.0.0.1:<port>/mtg-annual-and-cr-report-2021.pdf -> 2025 not on the first 3 pages and no
  accounting period for it; cover names 2021`,`fetch self-check ok`。拒绝原因与用例 20 的
  `tried` 断言逐字一致;文档不落盘断言同过。

## fixture

`backend/fixtures/mtg_fy2021_cover_pages.jsonl` = `git show 8a0e784~1:data/kb/modern_times_2025/pages.jsonl`
的前 3 行,**字节一致**(两侧 `git hash-object` 均 `13883af79f66d53e57c369100c6f9c823d90b71c`);
只含文字层文本(该条目本就来自 KB 的文字层解析)。用例 20/22(`mtg_head`)改读
`_mtg_fy2021_cover_pages()`;`_kb_page_texts("modern_times_2025")` 这条运行时依赖删除
(`_kb_pages`/`_kb_page_texts` 保留:用例 23 dustin、用例 25 随机正例仍在用)。

## 用例 25 取消排除

`stems.remove("modern_times_2025")` 移除,MTG 与其它 stem 一样进正例池;注释改述原因(8a0e784 重建后
封面即 2025,缺陷文本只活在 fixture 里;残余可疑 stem 由 `scripts/kb_year_audit.py` 列)。附带核实:

- 审计重跑(`kb_year_audit.py --kb data/kb`,零模型):206 家,2 家 suspicious(abb_2025 页眉年份形、
  saab_2025 纯图 PDF)—— modern_times_2025 已不在列(修复前 3 家)。
- seed 122 抽样随池子 +1 而变化:新抽中 nelly / clas_ohlson / roko / fasadgruppen / asker_healthcare
  (不再含 net_insight / rusta),均避开上述两家 suspicious,用例 25 绿。

## 门

- 11 套全绿(exit 0):`pipeline.test_fetch` + test_confidence / test_kb / test_parse / test_paths /
  test_llm / test_global_ask / test_locate + 根 test_workbench / test_human_review / test_collection。
- 所有者标识 grep(改动 + 新增文件,基线 cda9766)为空。
