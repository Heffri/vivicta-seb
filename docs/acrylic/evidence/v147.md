# v147 — README rows for v124/v136/v139–v145 + current KB/eval numbers; the shared test app refreshed to the current acrylic tip

Lane `ao/vivicta-glm-85/v147-docs-and-app-refresh`, baseline `origin/acrylic` — `eb1ec6c` at
dispatch; re-fetched before delivery it had moved to `8a13271` (v144: MTG's column-order total
must close on its own row, plus the `modern_times` data publish), merged in (fast-forward) and
everything below re-checked on the merged tip. Docs-only in-repo, zero model calls.

## README

Eight hardening-table rows, each linking its evidence, placed in version order: **v124** (the
v123 locator's live effect: label ✓ 9 → 14 of 24, and pass-1 page-select displaces the window
as the binding constraint — 16/24 label pages selected, 8 lost to risk-note pages),
**v136** (18-stem union rerun against v135's new labels: stored retained 15/18; the tiebreak
finding dispatched v145), **v139** (balance-sheet companion page + two corpus-narrow keywords,
debt label-page reachability 100/105 → 105/105), **v141** (a same-model rerun is a net loss on
reports with a stored answer — 14/23 raw union vs 20/23 stored; flerie's 2.1 the only publish),
**v142** (packaged walkthrough: no in-scope product defect, triage 0/1/2), **v143** (pass-1
selected 1/5 of the newly reachable pages — reachable ≠ selected; meren_energy published),
**v144** (a column-order read must close on its own row before overriding a non-null model
total; stored `modern_times` total 85,151 → null), **v145** (merge same-check-state conflict →
null, decided on the recorded per-run data, higher confidence 0/4). v144 was mid-flight when
this lane was dispatched — the work order's "若已合入" condition triggered at delivery time and
it got its row like the rest.

Numbers re-run for this lane, not copied — and re-run again on the merged tip (`8a13271`):
`ls data/kb` = **206** entries, **105** with a stored `debt_maturity` extraction;
`eval/run.py --stored-kb data/kb` = values **310/367 (84.5%)**, pages **249/311 (80.1%)** —
matching the work order's expectations, and v144's `modern_times` publish moves neither number
(a null answer to a non-null label is ✗ on both metrics either way; the row stays in the page
denominator). The label file holds 271 `debt_maturity` rows over 105 companies (verified by
count); the 367 scored rows are 271 debt + 96 income (99 income rows on file, 3
`nordic_industrials` rows unscored — no stored extraction). The Data bullet's 206/105 was
already current and stays.

## App refresh

`refresh-app.ps1 -Src <this worktree>` (via a wrapper `.ps1` so the script's not-running guard
could not match its own parent command line — the v140 lesson). App not running beforehand
(checked by process command line); the shared app dir was refreshed in place, twice:

1. from `eb1ec6c` (the tip at dispatch) in **257 s** (cold caches: fresh venv + PyInstaller +
   npm installs);
2. from `8a13271` (the merged tip, after v144 landed mid-lane and was merged in) in **63 s**
   (warm) — this is the delivered build.

Final `BUILD.txt`: `built 2026-09-20 15:11 from 8a13271c612307ddb2f2f36995eb7df49d2a34b0
(ao/vivicta-glm-85/root)`. Both runs replaced all four resources (frontend-dist, backend, data,
app.asar shell).

Double-click smoke of the delivered (`8a13271`) build (`--user-data-dir` under the lane's own
directory; backend on a random free port, found via the backend.exe PID's listening socket,
up 12 s after launch):

| check | expected | actual |
|---|---|---|
| `GET /api/kb` | 206 entries | **206 entries** |
| `GET /api/config` | `merge_runs` present (v140 echo) | `merge_runs=off`, plus `maturity_basis=carrying`, `provider=fixture` |
| `GET /api/kb/meren_energy_2025/debt_maturity` | `total_debt` 333.8 (v143's publish) | **333.8** @0.9, `due_within_1_year` **68.6** @0.9 (fields is an array; the longer buckets null at conf 0) |
| `GET /api/kb/modern_times_2025/debt_maturity` | `total_debt` null (v144's publish, freshness proof) | **null** @0.0 |

Shutdown: window close took the backend down with it (no forced kill); zero residual processes
under the app dir afterwards, only this lane's own PIDs touched.

Two launch gotchas for the next lane (both silent): the `--user-data-dir` **value must be
quoted** when the path contains a space (unquoted, Chromium reads only the prefix up to the
space as the dir), and the directory must **exist before launch** — otherwise Chromium's
process-singleton lock fails with `Lock file can not be created! Error code: 3` and the exe
exits 0 with no other symptom.

## Gates

- In-repo diff vs `origin/acrylic` (`8a13271`): `docs/acrylic/README.md` (+8 rows, the
  Accuracy line) and this file. No code, schema, eval or `data/` change of this lane's own
  (the merge brought v144's `extract.py`/`test_confidence.py`/`modern_times` data — that
  lane's work, merged verbatim).
- Owner-identifier grep over every changed file — run separately from the push, empty.
- Zero model calls; the packaged app ran in fixture/demo mode throughout its smoke.
