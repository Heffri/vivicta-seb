# v106 — README rows for this afternoon's landings + summary and eval numbers brought current

Lane `ao/vivicta-glm-46/v106-docs-rows`, off `origin/acrylic` at dispatch `45e7dc1` (the worktree's
own base; delivery fetch/merge re-checked below). Docs only — territory: `docs/acrylic/README.md`,
`eval/README.md` (the row-count sentence), `backend/README.md` (the publish_kb section), this file.
Zero model calls; no code, schema, `data/` or other-evidence change.

## What changed

- **Backend hardening table +8 rows**: v095 (off-grid boundary columns + debt-row-over-bare-Total),
  v096 (finer-split sibling sums), v097 (seed 10, measure-first, nothing landed), v101
  (`_normalize_sign`), v102 (December date-form year headers), v104 (Swedish finer-split forms +
  torn-row read), the supervisor's confidence clamp (`b25eb2d` — a derived value never also earns
  `value_in_quote`, clamp at 1.0), and the publish-republish mechanism (v093's `publish_kb.py` as the
  standing way `data/kb` follows the code, with the five `data:` commits).
- **Intro summary**: eleven rounds (seeds 1–10 + the Financials sweep) / 115 company draws; the
  reader families count four → five (`_finer_split_rows` joins); v097 joins the round-evidence list.
- **Frontend table**: the v100 Theme row landed with its own merge, so the remaining delta was the
  screenshot links — added to the row's Evidence cell (browser dark/light + the control, desktop
  switched-live + next-launch shots; all five pngs verified present).
- **"What a colleague sees now"** section (4 bullets): `desktop-demo` CI release / `desktop-0.3.4`
  zip; KB stock with the Wallenberg default (issue #4); the Theme switch; the eval numbers.
- **Assumptions**: #3 gains "v096's explicit sibling sums restore the values the flag had cost
  (MedCap)"; v101's sign normalization added as #6 ("Liabilities printed negative in net-debt
  presentations are recorded positive").
- **`eval/README.md`**: the stale "181 rows / 62 companies" sentence → 231 debt rows / 87 companies,
  plus why the scored count is 327 (96 income rows; the 3 `nordic_industrials` example rows never
  score).
- **`backend/README.md`** publish_kb section: the v093-era stock sentence marked historical, one
  sentence added for the republish-after-every-landing flow (five `data:` commits, the 1.0 publish
  clamp) and the current stock/eval numbers.

## Numbers (re-run / re-counted on the delivered tree, zero calls)

- `python eval/run.py --stored-kb data/kb --no-fail`: values **257/327 (78.6%)**, pages
  **200/327 (61.2%)** — matches the work order.
- `eval/labels.csv`: **231** `debt_maturity` rows / **87** companies (327 scored rows incl. 96
  income; 330 raw rows incl. the 3 `nordic_industrials` examples).
- `data/kb`: **196** stems, **95** with `extractions/debt_maturity.json` — the work order's "96 有
  debt" did not reproduce: v093's own documented stock is 85, +10 seed-10 entries = 95, and the
  count on `origin/acrylic` (`git ls-tree`) is 95 at every data commit since the seed-10 publish.
  The README/`backend` README/summary carry the counted **95**.
- Hardening draws: 105 + 10 = **115** ✓ (matches the work order).

## Gates

- All README/evidence links in the edited rows resolve (evidence v095…v105, the five v100 pngs).
- Owner-identifier grep over every changed file: empty.
- No build/lint gates triggered (no frontend/backend source change); `git status` clean apart from
  the three READMEs + this file.
